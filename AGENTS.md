# AGENTS.md — operating manual for the proof AI

You are proving real Python programs correct in Lean 4 (`leanprover/lean4:v4.33.0-rc1`, core only, no packages).

## Orientation

1. A Python file's real AST is dumped to a JSON envelope (`extractors/python/extract.py`, CPython `ast` frontend).
2. `load_program f from "….json"` ingests it at elaboration time: `f : Module` is a **literal AST term** proofs can unfold.
3. A fuel-based definitional interpreter (`LeanModels/Python/Semantics.lean`) gives it meaning. Since the H1 core re-shape (docs/memory-model.md v2) the mutual block is `Run`-typed over `FrameState` (`World` = heap + globals + stdout, plus this frame's locals; state retained on `.ok` AND `.exn`), **`callIn` is the mutual-recursion point**, and the public `callFunction m f args fuel : Res Val` is a NON-recursive wrapper — thaw (`RVal.thawArgs`, kernel-reducible) ∘ fresh-world ∘ `callIn` ∘ deep-freeze (`Run.toPublic`) — with its signature and `CallsTo` unchanged. FROZEN recursion points: `callIn`/`execWhile`/`execFor` (+ H4's `stepIter`/`execGen`/`execForGen`) — unfold one step with `rw [callIn.eq_2]` / `rw [execWhile.eq_2]` / `rw [execFor.eq_2/eq_3]`; `callFunction` itself unfolds freely (`unfold callFunction`, and it is in `py_simp`'s default set). Tier (2026-08-07, H1-proper): `while`/`if`/`for`-over-lists/assignment/tuple-unpack; builtins `len`/`sorted`/`max`/`min`/`abs`/`int`; G1 constant module globals resolved from `Module.topLevel`; and the DICT TIER (docs/memory-model.md §dict semantics) — literals (heap-allocated, `.ref` values), subscript read/write (`KeyError` faithful, aliasing-visible mutation), `len`, `in`/`not in` on dicts, `.get(k)`/`.get(k, d)` (the only method call in tier), `==`/`!=` through the heap (`heapEq`: identity shortcut, active-pair `RecursionError`, order-blind), truthiness, dynamic `is`/`is not` (refs by address; two non-`None` immediates stay loud). Loudly out: live dict iteration (`for k in d` — NO snapshot shortcut), `.keys/.values/.items/...`, `del`, comprehensions, `**kwargs`, membership on value containers, returning a dict through the boundary. Proof geometry: `worldInv` is CONDITIONAL on the kernel-computable heap-free fragment (`Expr/Stmt/Module.heapFree` — dict literals and subscript stores are the only non-members); pure `CallsTo` specs lift into the stateful `CallsIn` judgment via the frame theorem `CallsTo.callsIn_frame` (heapFree autoparam, `rfl` at concrete modules); `heapEq` is a FROZEN recursion point like `execWhile` (ref-free `==` takes the pure `valEq` fast path, so ordinary symbolic execution never opens it). G1 module init is HEAP-THREADING (top-level dict literals live in `initWorld`'s heap; the accumulator stores `RVal`s; post-boundary bindings are POISONED — a stale value would be silently wrong) and `initWorld` carries init heap + resolved globals (name resolution stays on the static `moduleGlobals` table — equivalent while bindings are immutable in tier). The stateful judgment `CallsIn` exists with concrete kernel-witnessed proofs (`Examples/python/sf_tt` — module-global dict mutation across nested calls); `Examples/python/sf_pst` evaluates sunfish's REAL piece/pst tables and MATE window through the tier (concrete theorems; symbolic dict reads await walker heap support). `leanpy` is built (v1, 2026-08-12): `tools/leanpy <file.py>` runs an ARBITRARY Python file end to end — extract (`extract.py --out`, cached, nothing written beside the source) → `leanmodels-run --script` → stdout and exit status forwarded (LeanModels/Python/Script.lean; loud exit codes 3/4 never count as agreement, 5 = `--compare` disagreement). `harness/leanpy_survey.py` is the completeness METRIC over corpora (`harness/leanpy_corpus.json`), one `--script-batch` process for the whole sweep, verdicts MATCH/DIVERGE/REFUSE/TIMEOUT/ORACLE/EXTRACT/HUNG, oracle pinned to `python3.9` and printed, dynamic refusal + static `Unsupported`-by-`py_kind` telemetry, and a `live` count so a definitions-only agreement is never counted as a real run; `harness/script_corpus.py` remains the pinned regression corpus. leanpy's module-order admission is PER NAME (`defsBoundBefore`, 2026-08-12): a top-level statement may mention a name the module binds by `def`/`class`/namedtuple only if that definition ends before the statement begins — the blanket "every def precedes all live code" rule is gone (it was the top blocker of real Python, 146 of 158 stdlib refusals), and the check now covers the G1 PREFIX too, where `x = f()` above `def f` used to answer 1 where CPython exits 1 with NameError. `__name__` IS SUPPLIED in script mode (2026-08-12): a file run as a program has `__name__ = "__main__"`, prepended to the prefix view as the first MARSHALLED GLOBAL of the runner boundary (the argv family), so `if __name__ == "__main__":` blocks run; every other module dunder stays loud, and the closed-function surface never sees the binding. THE ONE PIPELINE (2026-08-13, docs/memory-model.md §the one pipeline): leanpy's prefix/suffix SPLIT IS GONE — `runScript` executes EVERY top-level statement through the script executor from an EMPTY world and publishes the frame's locals as `World.globals` after each statement (CPython's module frame, whose locals ARE its globals; keeping the locals rather than draining them is what makes a module-level `x += 1` read the global, since `execStmt`'s augAssign arm is locals-only by CPython's compile-time rule), so `initWorld` is never called in script mode and nothing can be skipped, rolled back, or read stale. The world-symbolic covenant is kept by NOT touching resolution: the threaded module VIEW (`scriptView`) carries no program statement — only `__name__` (the dunder arm fires before the live consult), an unnameable top-level `def` (so `topLevelDefFree` is false and closure calls take the dynamic path), and the module's imports (so the benign whitelist still poisons `time` and `moduleClockOk` still holds) — hence every module global is statically ABSENT and the absent arm's live-view consult decides, making sequential visibility exact for free. DELETED, not tightened: `initNothingSkipped` (which had guarded the `x = talk()` / `x = 1 // 0` rollback holes, with a recorded residue now closed by construction), `suffixConsistent`, `g1Shape`/`g1Prefix`/`liveSuffix`. The one residue is `scriptFlushCoherent`: a compound statement DELEGATED wholesale to `execStmt` (`for`, `try`, `while … else`) holds its bindings until it ends, so a name it assigns may not be one a function reads (control shells exist for `if`, `while`, and `for … in d.items():`). Measured: in-repo corpus 69/86 MATCH (80.2%, from 68.6%), files executing live top level 18 → 47, 0 DIVERGE; script corpus 21 matched / 6 loud (from 18/9); `sf_order.py` REFUSE → MATCH. TWO BUGS IT EXPOSED, both pre-existing on the closed-function surface: (1) FIXED — an unmodelled CPython builtin answered `NameError`, a WRONG ANSWER (`dict(…)` in the shipped sunfish, and `len(dict())` from any function body); `isPyBuiltinName` is now the pinned CPython 3.9 `dir(builtins)` and every arm that may DECIDE a `NameError` consults it first, refusing loudly instead. (2) FIXED — `moduleGenFree` claimed no `Obj.generator` can exist without a generator def, but `enumerate`/`itertools.count` allocate generator FRAMES, so `for i, c in enumerate(s):` in such a module refused with an `internal: … report this` message (loudness defect only — `Expr.heapFree` already excludes those calls, so `worldInv` was unaffected); it now also requires `genAllocFree` over function bodies AND the top level (`Expr.genAllocFree`: no `enumerate`/`count` call, no surviving genexp), with LIST-recursive walkers because `Module.heapFree` is discharged by `rfl` and `Array.all` does not reduce in the kernel, and the SCRIPT VIEW's marker `def` carries an `enumerate()` call so the shortcut is off there too (a view with no program statement cannot see the program's own calls). Acceptance: `iter_lab.enum_sum`/`enum_first` + `#guard !moduleGenFree iter_lab`, `harness/scripts/enum_script.py`. Both harnesses now also compare the exception CLASS — CPython maps every uncaught exception to exit 1, so stdout+exit alone counted sunfish's `NameError` vs CPython's `ModuleNotFoundError` as agreement. PRINT IS AN ORDINARY BUILTIN (2026-08-13, docs/memory-model.md §`print` is an ordinary builtin): `evalExpr`'s call arm appends one chunk to `World.stdout` and returns `None`, after every shadow-resolving arm (locals, module globals, `findFunction`/`findClass`/namedtuples), so `print` works INSIDE FUNCTION BODIES and inside any statement leanpy delegates — the script executor no longer intercepts `print` at all and the "prints inside a top-level `for` are loud" gap is gone; `strOfRVal`/`strOfArgs` moved into Semantics.lean, the printable tier is unchanged (containers/heap values refuse rather than guess a `repr`), `print(x, end="")` stays loud through the kwargs arm. `print` LEAVES `Expr.heapFree` (the first world-mutating non-allocating expression — `worldInv` gains `hprx` and loses one walked `ite`, `fuelMono`'s branch becomes a `bind`, `clockErase` gains a `bprint` case closed by `of_seed`). RECORDED: the VALUE boundary does not observe stdout — `callFunction` drops the world, so `CallsTo` is silent about output; `CallsIn` and the script surface are where it is seen (`g1_lab.try_print` says so). In-repo corpus 72/87 MATCH (82.8%), 0 DIVERGE; script corpus 24 matched / 4 loud. LIST COMPREHENSIONS ARE LIVE (2026-08-13, docs/memory-model.md §list comprehensions): the extractor emits `ListComp` as the SAME node as a generator expression under a different `kind`, INGESTION desugars `[e for x in it if c]` into `list(e for x in it if c)` (CPython's own compilation — the `yield from` inlining precedent, so the envelope stays a faithful AST dump), and the genexp lowering carries over WHOLE (capture census, walrus filter, `drainOk` gate — which already counted `list` as draining). The one new interpreter piece is `list(iterable)`: `tuple`'s exact inventory of iterables but it ALLOCATES (CPython's `list(x)` is never an alias), so the call leaves `Expr.heapFree` — `worldInv` gains `hlstx`, `fuelMono` a `bind` arm, `clockErase` a `case blist` over the new `ClockErasedF.allocList` leaf lemma — and its generator arm drains WITHOUT the `moduleGenFree` guard `tuple` needs, precisely because it is outside the fragment. Acceptance: `iter_lab.lc_squares`/`lc_filter`/`lc_capture`/`list_of`/`list_empty` + `harness/scripts/listcomp_script.py`; the next wall in its place is `print` of a CONTAINER (a list's `repr` is not guessed — `print_container.py`). In-repo corpus 73/88 MATCH (83.0%), 0 DIVERGE; script corpus 25 matched / 4 loud; 998 differential cases, 0 failed. CONTAINERS RENDER (2026-08-13, docs/memory-model.md §rendering): `print` applies CPython's TWO-LEVEL rule — `str()` on the arguments, `repr()` on everything inside a container (`printOne`/`reprVal`, replacing the scalar-only `strOfRVal`) — covering int/bool/None, str (CPython's quote choice plus `\\`/quote/`\n`/`\r`/`\t` and `\xNN` for the other C0 controls and DEL), tuple (one-element trailing comma), namedtuple (`Move(i=1, j=2, prom='')`), list, dict (insertion order), range (step elided at 1), and `[...]`/`{...}` for a self-referential container decided by an ACTIVE-PATH list, not fuel (depth beyond the fixed `reprFuel` refuses). LOUD: a set (hash order), an instance/closure/generator (identity), a non-ASCII string (Unicode printability is never guessed — `print("héllo")` is fine, `print(["héllo"])` is not). No proof-layer movement (`print` already left `Expr.heapFree`; `reprVal` is pure). Rows: `repr_script.py`, `print_set.py`, `print_nonascii.py`. In-repo corpus 74/90 MATCH (82.2%), 0 DIVERGE; script corpus 26 matched / 5 loud. THE `dict(…)` CONSTRUCTOR (2026-08-13, docs/memory-model.md §the `dict(…)` constructor): `dict()`, `dict(k=v, …)` in the call's own order (duplicate keywords are a SyntaxError, so no insert collides), `dict(d)` as CPython's shallow COPY; a pairs ITERABLE stays loud (per-insert hashability and duplicate-key rules are not guessed). It allocates, so it leaves `Expr.heapFree` (`worldInv` gains `hdctx`, `fuelMono` two arms — positional and keyword — `clockErase` a `bdict`/`bdictkw` pair over the new `ClockErasedF.allocDict`). THE PAYOFF: it was the SHIPPED sunfish.py's last named blocker, so `runScript` now executes the real engine file's ENTIRE top level (imports, piece/pst, the padding loop through the items shell, K_MID/K_END, the namedtuples, all three class creations, `hist`, and the `__main__` guard, which is TRUE under leanpy) and stops at exactly one construct — `main()`'s `import sys, sunfish_ui.uci`, out of scope BY KIND, which CPython does not survive either (ModuleNotFoundError). Pinned BY MESSAGE in `Examples/python/sunfish/pins_init.lean`, which therefore imports `LeanModels.Python.Script` directly (`runScript` is not in the umbrella). In-repo corpus 75/91 MATCH (82.4%), 0 DIVERGE; script corpus 27 matched / 5 loud; 998 differential cases, 0 failed; stdlib sweep 5 MATCH / 0 DIVERGE. H2 LIST TIER (2026-08-07, the in-world half): every list the interpreter BUILDS is a heap object (`Obj.list` — literals allocate per display, G1 module-level tables allocate into `initWorld`, `sorted` allocates its fresh result); in tier: list subscript read/WRITE (negative indices; faithful `IndexError`), `len`, truthiness, `in`/`not in` (the fueled `element == probe` scan through `heapEq`), `==`/`!=` (identity shortcut, active-pair `RecursionError`), dynamic `is`, `.append(x)`/`.pop()`/`.pop(i)`, unpacking from a heap list, and `for` over a heap list as a LIVE INDEX CURSOR — `execForList`, a FROZEN recursion point (`rw [execForList.eq_2]`); body mutation/growth/`pop`-shrinkage are observed exactly as CPython's listiterator. BOUNDARY (recorded gap): `callFunction` still thaws `Val.list` ARGUMENTS to the transitional `RVal.listV` (reads in tier; mutating a boundary-passed list refuses loudly) — the flip to the heap-threading `RVal.thawH` is built-but-not-wired, pending the list-example proof rebase (docs/backlog.md); RETURNED heap lists DO freeze to `Val.list` snapshots (`Run.toPublic fuel` = `RVal.freezeB`: structural boundary freeze, fuel consumed only on `.ref` paths via the frozen `freezeH` — cycle refusal by active path, DAG sharing duplicated). Stateful exemplars: `Examples/python/sf_hist` (`CallsIn` at `.ref` ARGUMENTS — the aliased-vs-distinct `poke_first` pair is the acceptance behavior as a theorem, plus a value-symbolic aliasing proof by `py_simp` through `callIn`) and `Examples/python/list_lab` (the checks-only acceptance battery). Proof-layer geometry: the value-side bridges (`PyTriple.callsTo*`, `CallsTo.toTriple`, `callsTo_iff_triple`, `EvalsTo.call`, `CallsTo.callsIn_frame`) carry `Val.listFree` side conditions (autoparam `rfl`; the walker discharges them like `Module.heapFree`); `RVal.freezeB_thaw`/`RVal.eq_thaw_of_freezeB` are the boundary roundtrip/inversion; `Expr.heapFree` excludes list displays, every `sorted` call, and `.append`/`.pop` method calls (allocating/mutating); `RVal.typeNameH` is deliberately OUT of `py_simp`/`interpUnfolds` (recorded finding: as a simp member it whnf-storms plain symbolic runs — see its docstring). The `for` proof pattern (frozen-tail list induction via `execFor_mono`, hand-unrolled first iteration) is `Examples/python/sf_bound_for/proof.lean`; `py_vcgen`/`py_loop` do NOT walk `for` loops yet — manual route only. Stage-1 facts the proof layer leans on: `worldInv` (nothing allocates ⇒ decided runs return their input world) and the thaw/freeze roundtrip (`RVal.freeze_thaw`/`RVal.eq_thaw_of_freeze`). H3 CLASS TIER (2026-08-08, docs/memory-model.md §class semantics): `ClassDef` structured end to end; ingestion FLATTENS methods into `Module.functions` under `"<class>.<method>"` qualified names, so METHODS ARE FUNCTIONS — method calls run through `callIn` with `self` as an ordinary `.ref` first argument and `CallsIn` specifies them verbatim (`Examples/python/sf_searcher`: `CallsIn m w "Searcher.bound" #[.ref 0, …] w' v` — the cross-call table write/read pair, chained and pinned); `ClassId` = the `Module.classes` index (never the name; last-wins consistently; def/class name collisions loud). Instantiation allocates `Obj.instance` and runs `__init__` through `callIn` (non-`None` result and `__init__`-less arity are faithful `TypeError`s). THE DUNDER GUARD: classes with bases/metaclass/decorators/class-level statements/dunders-beyond-`__init__` are represented but UNINSTANTIABLE (loud), so every live instance has default protocol; CLASS CREATION IS A SECOND, INDEPENDENT QUESTION (2026-08-12, found by leanpy): CPython runs a class body and evaluates bases AT the `class` statement while the model executes neither, so `ClassDefn.creationPure` (extractor `creation_effects` + ingestion's re-check) gates `runScript` — a class whose creation could print or raise refuses the whole script, while a creation-pure-but-uninstantiable class (`class Tag: kind = "tag"`) still lets the program run — instance `==` (identity), truthiness (`True`), `is`, and the default-protocol `TypeError`s (len/subscript/iterate/unpack/membership/sorted/max/min) are faithful by construction. Attribute reads: instance attrs → bound-method-value loud → faithful `AttributeError` (`PyErr.attributeError`, new — also faithful now: `.get` on lists, `.append` on dicts, attribute stores on dicts/lists/scalars; a missing attribute in call position raises BEFORE arguments, CPython order); attribute stores mutate in place (RHS → primary → store). Instances as dict keys are loud (`keyRefusal` — identity hash, never a fake unhashable `TypeError`); instances cannot cross the boundary (freeze refuses). RECORDED FINDINGS: (1) referent dispatch must fork on a PURE free-scrutinee plan (`attrReadPlan`/`attrCallPlan`/`attrReadResult`; `execAttrCall` is a NEW MUTUAL-BLOCK function — `fuelMono`/`worldInv` conjuncts appended LAST so existing projection paths survive) because a match nested under the receiver's binder is invisible to `cases`/`rw` in meta proofs; (2) `cases` never abstracts HYPOTHESES — use `split at h` (see `attrReadResult_ok`); (3) `Module.heapFree` now also requires `classes = #[]` (instantiation allocates; syntax cannot tell a class call from a function call), and the frame theorems ride `findClass_heapFree`/`getClass?_heapFree`/`attrCallPlan_get_heapFree`. Acceptance battery: `Examples/python/cls_lab` (21 functions, all differential); leanpy corpus: `harness/scripts/class_script.py`. SYMBOLIC method-state proofs (`sf_searcher`): `bound_writes_symbolic`/`bound_reads_symbolic` — the cache-hit read is HYPOTHESIS-FREE (`keyEq` reflexivity decides the symbolic tuple-key lookup); proof pattern: `refine ⟨64, ?_⟩; rw [callIn.eq_2]; py_simp [prog, execAttrCall, worlds…, facts…]` per call — the whole-driver route (unfolding `callFunction` + several nested `callIn`s in one `py_simp`) whnf-storms; census: the real sunfish.py's three ClassDef bodies all extract structurally (`Searcher` with `class_unsupported: null`). NAMEDTUPLE VALUE TIER (2026-08-08, the recorded VALUE-like decision IMPLEMENTED — docs/memory-model.md §class semantics): `RVal.ntuple tname fields xs` is an IMMEDIATE value carrying its own class/field names (no table at access time, no allocation — heap-free modules keep namedtuples); recognition is at INGESTION (Json.lean, ALL-OR-NOTHING per module: `X = namedtuple("T", <fields>)` under the exact benign import `from collections import namedtuple`, CPython-validated field specs, a binding census over every top-level statement, `namedtuple` referenced nowhere else, and NO def/class subtree containing `global` — the extractor-recorded `has_global` field makes that check exact under opaque `try`/`with`; recognized statements become `pass`, `Module.namedtuples` fills, anything else leaves the module unchanged/loud); construction is exact-arity (faithful `TypeError`), field access is tuple indexing (`ntupleAttr`: unknown attrs the faithful `AttributeError`; the CPython protocol `_replace`/`_asdict`/`_make`/`_fields`/`count`/`index` and dunders EXIST there — loud), equality/hashing/len/subscript/iteration/unpacking/max/min/`+`-concat are class-ERASED tuple semantics (`Move(1,2,"") == (1,2,"")`; one dict slot — the `tp_score` pattern, proved symbolically in `Examples/python/sf_position` incl. an equal-but-distinct reconstructed Position key), and the BOUNDARY refuses namedtuple results loudly (a `Val.tuple` snapshot would forget the class and falsify freeze inversion — `CallsIn` is the surface that sees the values). The real sunfish.py's `Move`/`Entry` recognize as-is. H5 SLICE 1 (2026-08-08): the value-like SUBCLASS shape — `class Position(namedtuple(…))`, the shipped sunfish Position — is recognized structurally (extractor `namedtuple_base` → `ClassDefn.ntBase`, promoted/demoted by the SAME census) and INSTANTIATION constructs the immediate value carrying the SUBCLASS name (no allocation; `__init__` on the immutable self loud; bound-method READS loud via `ntupleMethodName` — never a fake `AttributeError`); the real sunfish.py's `Position` now constructs (`ok = true`, six-field base). H5 SLICE 2 (2026-08-08): METHOD CALLS on the immutable self are LIVE — `ntupleCallPlan` (methods shadow base field properties, CPython MRO; field-in-call-position/protocol/dunders loud; missing the faithful pre-args `AttributeError`) dispatches the flattened qualified function through `callIn` with `self` = the ntuple VALUE; dispatch identity holds because the census ALSO refuses a plain candidate whose TYPENAME collides with an `ntBase` class name (hand-built modules keep the loud-only caveat, plus `ntupleCallPlan_heapFree` keeps `worldInv` vacuous). Exemplars: `position_mirror_callsIn` (the method AS a `CallsIn` theorem, symbolic score) and `mirror_score_callsIn` (surface dispatch; per-call `callIn.eq_2` geometry — whole-driver `py_simp […, callIn]` storms even loop-free). RECORDED FINDINGS: (1) `py_simp` on a comparison whose rhs is still SYMBOLIC under the chain's binder must ERASE `valEq`/`valEqList` (`py_simp […, -valEq, -valEqList]`) — the equation-lemma unfolding at a (constructor, variable) pair produces a KERNEL-REJECTED `Eq.refl` (smart-unfolding mismatch; `sf_position`'s `move_eq_symbolic`); (2) a whole-loop `py_simp [execFor]` over a concrete namedtuple can blow the simp step budget — step the frozen point per element (`rw [execFor.eq_3]` per iteration, `eq_2` at nil); (3) `py_vcgen` reads `FunctionDefn.mk`'s BODY at positional field 5 (VCTactic.lean — keep in sync when Ast.lean fields change); (4) the extractor's static-locals census now collects only REAL binding names (`_target_bound_names`) — walking whole subscript-target trees falsely shadowed callees inside subscript keys (`tp[(Move(i,j,""),1)] = …`). leanpy corpus: `harness/scripts/namedtuple_script.py`; differential rows: `sf_position` (10 match + 3 whitelisted-loud). H5 STRING TIER (2026-08-09, docs/memory-model.md §string semantics): slices STRUCTURED end to end (`Expr.slice` — extractor `Slice` node; absent bounds ingest as `Constant None`, CPython's own BUILD_SLICE compilation; evaluation receiver→lower→upper→step; validation step-FIRST per `PySlice_Unpack` — `str_lab.order_probe`) and CPython-exact on every str (negative/clamped bounds, both step directions); str METHOD dispatch through the pure `strCallPlan` (the `ntupleCallPlan` free-scrutinee discipline): `.swapcase()`/`.isupper()` ASCII-exact (a non-ASCII string is loud — Unicode tables are not guessed), `.index(sub)` code-point-exact (faithful `ValueError` miss; non-str argument faithful `TypeError`; `start`/`end` loud); EVERY other str attribute is loud, never a fake `AttributeError`; list/tuple/namedtuple slices loud (they ALLOCATE); slice STORES loud. SIMP DOCTRINE: the dispatchers (`sliceVal`/`strCallPlan`) are in `py_simp`/`interpUnfolds`, the WORKERS (`strSlice`/`strSwapcase`/`strIsUpper`/`strIndex` + helpers) deliberately OUT (the `sortInts` freeze family) — concrete proofs rewrite through kernel `rfl` facts, stated with LITERALS everywhere (a folded `def` in a fact's LHS does not match the simp-normalized goal), one rewrite per 120-char worker application; the `rfl` elaboration needs `set_option maxRecDepth 131072`, and a doc comment cannot precede `set_option … in theorem` (parse error — use a plain `/- -/` comment). `Expr.slice` is IN `Expr.heapFree`; str-method CALLS conservatively leave the fragment (the attribute whitelist is still `.get`-only, like every `sorted` call — recorded gap, docs/backlog.md). H5 ITERATION (2026-08-09, docs/memory-model.md §iteration semantics): `in`/`not in` are decided for EVERY in-tier container by ONE dispatcher `valContains` — heap referents delegate to `heapContains`, a str is CPython's SUBSTRING test (`strContains`; a non-str left operand is the faithful `'in <string>' requires string as left operand` `TypeError`), value tuples/boundary lists/namedtuples scan elements through the fueled `heapContainsScan` (class-erased); `for` over a str walks its CODE POINTS (`strCharVals` — the snapshot IS the live semantics: strs are immutable); `ord`/`chr` are code-point exact (a surrogate `chr` is refused LOUDLY — Lean's `Char` cannot represent one). Simp doctrine unchanged: the dispatchers (`valContains`/`ordVal`/`chrVal`) are in the sets, the workers (`strContains`/`strCharVals`) are OUT. All of it stays IN `heapFree` (pure reads). Acceptance: `Examples/python/iter_lab` (checks-only, every function differential); corpus `harness/scripts/iter_script.py` (note: prints inside a top-level `for` are still loud — leanpy's live-suffix shell has a `while` case only). H4 GENERATORS (2026-08-09, docs/memory-model.md §generator semantics): generator functions are LIVE. `Obj.generator qname locals cont status` is a HEAP object (a generator is IDENTITY — `h = g; next(g); next(h)` advances one shared frame, and `break` must leave the very object the next consumer resumes; both pinned differentially in `Examples/python/gen_lab`), whose continuation is DEFUNCTIONALIZED: a stack of `GenFrame`s (`block`/`forSeq`/`forList`/`forGen`/`whileLoop`), each holding a structural SUFFIX of the body plus that loop's residual state — so `yield` is admitted in STATEMENT position only (`Stmt.yieldStmt`; the extractor's `is_generator` is CPython's scope-local syntactic rule). Loop frames carry NO "rest of block" (the frames below are it), which makes `break` = pop-the-loop-frame and `continue` = re-enter-it, both pure (`genBreak`/`genContinue`). NEW FROZEN mutual members: `stepIter` (step to the next yield; sets `running` so re-entry is the faithful `ValueError`), `execGen` (the continuation walker), `execForGen` (the lazy `for` cursor) — `.eq_n` one-step unfolds as usual, and their `fuelMono` conjuncts are appended LAST. `callIn` on a generator def runs NO code (allocate + return the ref). RECORDED FINDINGS: (1) `execGen` MUST fork on the pure plan `genPlan : Stmt → GenPlan` — with the statement match inline, the equation compiler splits the block arm per `Stmt` constructor and `simp only [execGen]` never fires at a symbolic statement (the H3 free-scrutinee finding, now load-bearing); (2) the whole mutual block now carries EXPLICIT `termination_by structural fuel` on every member — adding the three generator members made structural inference fall back to well-founded recursion, which silently broke every kernel `rfl` (`#py_check`, `py_vcgen`'s captured runs): the mergeSort trap, caught by `Tests.lean`; (3) `FunctionDefn` gained `isGenerator` BEFORE `body`, so `py_vcgen` now reads the body at positional field 6 (VCTactic.lean); (4) the VC bridges (`callsTo_arityOk`, `exists_callsTo_arityOk`, `Raises.toTriple`, …) gained an `f.isGenerator = false` hypothesis — `rfl` at concrete modules, an autoparam on `CallsTo.toTriple`. `Module.heapFree` gains a THIRD conjunct `moduleGenFree` (creation allocates; syntax cannot tell), `next(…)` leaves the fragment syntactically like `sorted`, and the `for`-over-`.ref` generator arm GUARDS on `moduleGenFree` — that guard is what keeps `worldInv` free of a heap-side invariant. Loud, never faked: `send`/`throw`/`close`, a `yield` in expression position, finalization, `x in gen` / `sorted(gen)` / `max(gen)` / unpacking (all CONSUME), a generator as a dict key or across the boundary, and generator EXPRESSIONS (`Expr.genExp` — structured, not yet lowered; docs/backlog.md). Generator EXPRESSIONS are LOWERED at ingestion (`lowerGenExps`, Json.lean; pass 7 adds THE WALRUS FILTER — docs/memory-model.md §the walrus filter: a filter `(v := e) <op> rhs` becomes `v = e` in the synthesized body, `v` a frame-LOCAL admitted only when the enclosing body never mentions it (PEP 572 leaks it there; `walrusForbidden`), a walrus anywhere else loudly unsupported) into synthesized generator functions `<genexpr@n>(.0, captures…)` — CPython's own compilation; the CAPTURE rule is by-value, so a free name is admitted only when by-value provably equals CPython's by-reference: a PARAMETER the enclosing body never assigns, or a name resolved outside the frame (module-level binding / builtin). Anything else leaves the node and refuses loudly. `enumerate`/`itertools.count` are generator FRAMES (`enumSeq`/`enumList`/`countFrom`), not a second object kind, so one stepper serves everything — `count` never exhausts (sunfish's ray). Acceptance: `Examples/python/gen_lab` (checks-only; two INFINITE generators that terminate only under real laziness) and corpus `harness/scripts/gen_script.py`. RECORDED BLOCKER for the shipped-file `gen_moves` theorem: it is NOT generators — every module-level global of sunfish.py is POISONED, because `globalsStep` sets `complete = false` at the first statement it cannot analyse (the three leading imports) and poisons every later binding, so `directions`/`A1`/`N`… are unresolvable. docs/backlog.md carries the designed fix (a dirty-NAME set instead of one global flag). THE SHIPPED FILE (`Examples/python/sunfish`: sunfish.py BYTE-IDENTICAL to the engine repo, 955KB envelope): the ingestion census is green on the real file (`Move`/`Entry` recognized; `Position` ok with its six-field ntBase; all 11 defs/methods extract with plain positional params — since H7's census refinement `Position.move` passes `localsOk` too; its `put` LAMBDA still refuses loudly at evaluation), and the headline theorems land — `rotate_callsIn`/`rotate_null_callsIn`/`rotate_home_callsIn`: `Position.rotate` score-negation on the REAL opening board, symbolic in the score AND the world (`CallsIn sunfish w "Position.rotate" #[posB s] w (posR s)` for every `w` — rotate touches neither heap nor globals, so the world is a free variable, strictly stronger than a pinned `initWorld` statement). Proof geometry: the `position_mirror_callsIn` transplant — `refine ⟨64, ?_⟩; rw [callIn.eq_2]; py_simp [sunfish, value defs…, hrev, hswap]`; the 955KB module literal unfolds WITHOUT storming (~5s/theorem). Acceptance: `Examples/python/str_lab` (checks-only battery, every function differential); corpus `harness/scripts/str_script.py`; `sf_position` gains the verbatim `rotate` method + wrapper rows (the assembled method against CPython). H6 (2026-08-10, docs/memory-model.md §call-site keyword arguments + §draining consumers): call-site KEYWORDS are structured end to end (`Expr.call` carries `kwargs`; binding is the pure call-site merge `mergeKwArgs` onto a COMPLETE positional array — `callIn`'s signature untouched; coverage: module defs + ntuple-subclass methods, faithful binding TypeErrors gated on `argsOk`, everything else loud; keyword-bearing calls leave `heapFree`); the DRAINING CONSUMERS are live — `sorted`/`max`/`min` drain a generator through the frozen `drainIter`, `any`/`all` (new builtins) short-circuit through `anyAllIter` leaving the generator SUSPENDED (both conjuncts appended LAST); ONE ordering relation `rvalLt` (tuples/namedtuples lexicographic class-erased, strs by code point, bools KEPT as bools) serves the `<` operator and the sorts; `sorted(reverse=True)` is descending STABLE insertion (never sort-then-reverse), `key=` stays loud (gates on first-class callables); str tier adds `.islower()`/`.upper()`; `any`/`all` are excluded from `heapFree`, `max`/`min` stay in it behind the `moduleGenFree` guard. Acceptance: `kw_lab`, `drain_lab`, and `sf_order` — the shipped ordering line `sorted(((pos.value(m), m) for m in pos.gen_moves()), reverse=True)` VERBATIM over verbatim `gen_moves`/`value` with the oracle-generated padded `pst`, whole ordered move lists differential against CPython. H7 (2026-08-10, docs/memory-model.md §nested defs and closures): NESTED DEFS are live under the SNAPSHOT tier — a def DIRECTLY inside a function body captures enclosing locals at def time, admitted exactly when no capture is rebound after the def (where snapshot ≡ CPython's cell, generator bodies included); extractor kind `NestedDef` with capture set (`Stmt.defStmt` carries the function INLINE — no flattening, no qname), execution allocates `Obj.closure` (heap identity; `==` False between distinct closures is EXACT), calls go through the frozen `callClosure` (conjunct LAST; `callIn` untouched), generator closures allocate the H4 object with the snapshot in stored locals. LOUD outside: rebound-after-def (`closure_lab.rebound_after` is the cell-vs-snapshot exposing row — refused, never translated), `nonlocal`, defs in loops or below the body's top level, calls of the nested name before its def (UnboundLocalError via `locals_unsupported`), closure kwargs, closures at the boundary. The closure-call arm guards on `funsHeapFree` (worldInv never meets it). The shipped `moves()` ADMITS (captures depth/gamma/pos/root/self/val_lower); capstone `sf_order.bound_probe` runs the moves()-shaped nested generator with the verbatim ordering line and the beta cutoff abandoning it mid-drain ((46,1) cutting vs (46,20) not). BOUND() ARC PASS 1 (2026-08-10, the mechanical blockers — docs/memory-model.md §set semantics + the lambda addendum under §nested defs and closures + the instance arm under §call-site keyword arguments): (1) LAMBDAS — a single-target `name = lambda …: expr` assign directly in a function body extracts as the H7 `NestedDef` (body = one `Return`; same capture census, a lambda's locals being its parameters only; same never-rebound admission, `closure_lab.lam_rebound` the refused exposing row; anything fancier stays the loud un-rewritten Lambda expression), so `Position.move`'s `put` ingests and the shipped census has NO refusals left. (2) INSTANCE-method call-site keywords (`self.bound(…, root=True)`): the `attrCallPlan .instMethod` path binds by the SAME pure merge with the `.ref` receiver prepended, `argsOk`-gated; builtin heap-receiver methods with keywords stay loud. (3) SETS, the honest subset (`Obj.pyset`, heap identity, first-seen order UNOBSERVABLE): construction from str/tuple/ntuple/list/set/GENERATOR (drains; `set` leaves `heapFree`) deduplicated by value equality under the dict-KEY doctrine (`set([1, True])` is one element; unhashable elements AND probes take `keyRefusal` — CPython hashes the probe before comparing, empty set included), membership/`len`/truthiness admitted (order-blind), EVERYTHING else loud (iteration/`sorted`/`max`/`min`/mutators/`==`/boundary — hash order is never guessed); sets are themselves unhashable (faithful TypeError as dict keys). (4) `self.tp_move.get(pos)` — dict `.get` through an instance attribute with a Position key — was already in tier and is now CLAIMED by differential row. Acceptance: `set_lab` (checks-only), kw_lab/closure_lab extensions, and the `sf_order` capstone probes — `move_probe` (the verbatim `Position.move` + rotate through the `put` lambda) and `killer_probe` (bound()'s moves() PROLOGUE: `killer = self.tp_move.get(pos)` keyed by the Position itself, the `if killer and pos.value(killer) >= val_lower:` gate, then the ordered tail with the beta cutoff). DEFERRED, flagged in the backlog, deliberately not started: exceptions (`raise Stop`, the driver try/except) and `time.time()` impurity (needs a recorded abstraction decision, never a silent stub). BOUND() ARC PASS 2 (2026-08-10): (1) THE SCRIPT-CORPUS REGRESSION FIXED — bisected to the dirty-name pass (6a79764): retroactive per-name poisoning is right for the closed-function surface and WRONG for statements `runScript` is about to EXECUTE; `runScript` now folds `initWorld` over the PREFIX VIEW (`mPre`, `topLevel := g1Prefix …`) and threads `mPre` through the executor, `suffixConsistent` widened to refuse ANY suffix assignment to a function-read name (a fresh suffix global would fake a `NameError` under the always-analysable prefix view; the stale-read half was already refused), and the top-level `print` arm probes the script LOCALS for a live shadow; corpus 19 scripts 0 failed / 14 matched / 5 loud (`suffix_rebind_read`/`suffix_fresh_global_read` are the refusal rows). (2) RECURSION THROUGH THE CAPTURED RECEIVER (memory-model §nested defs and closures, recursion addendum) — `-self.bound(pos.move(move), 1 - gamma, depth - 1)` from the resumed `moves()` frame is the COMPOSITION of built pieces, ZERO interpreter changes, and the pass is design + battery + capstone: `cls_lab.method_rec`/`method_mutual` (direct/mutual method recursion, one shared `nodes` across the nest), `closure_lab.gen_rec` (the bound() shape at lab scale — (2,4) under cut=2 vs (9,13) under cut=100, pruned subtrees never ran), `closure_lab.rec_nested_name` the refusal (a nested def calling ITSELF by name — census: no binding before the def; sunfish recurses through the METHOD name, `Module.functions` dispatch, not a cell), and capstone `sf_order.rec_probe` — MiniSearcher.bound with the verbatim ordering line inside `moves()` (captures = the shipped set minus `root`), recursive yields, beta cutoff pruning the recursion TREE ((113,7) vs (113,15) at depth 2), kernel-checked and differential. Battery 766 rows / 0 failed / 40 whitelisted. Mechanical note: the sf_order envelope outgrew the elaborator default — `set_option maxRecDepth 100000 in load_program` (the sunfish example's pattern). PASS 3 (2026-08-10/11, docs/memory-model.md §module-init execution): MODULE INIT EXECUTES. `initWorld` is now the LIVE PIPELINE `initFoldLive` — per statement, the pure fold step on the live state; fold-refused statements EXECUTE through the interpreter (`initExecStmt`, fixed `initExecFuel`) under the per-statement PREFIX VIEW `{ m with topLevel := done.push s }` (sequential name resolution — the full table would serve FUTURE bindings; the executing statement is IN the view so its dirty names are statically poisoned, not absent); the dict-items `for` gets the ONE control shell `execStmt` cannot express (`initItemsLoop` — live entries re-read per step, size change the faithful `RuntimeError`); failed attempts ROLL BACK and poison the LIVE accumulator too (`globalsDirty` on both views — a rollback without the marker would resurface a stale value). NAME RESOLUTION: static-first is UNCHANGED (world-symbolic proofs keep their geometry); the statically-POISONED and statically-ABSENT arms now consult `st.world.globals` — the live view — before refusing/NameError, and the CALL arms there dispatch live CLOSURES through `callClosure`, guarded by `funsHeapFree && topLevelDefFree` (the NEW 4th `Module.heapFree` conjunct — module init can create closures, so the fragment must exclude def-bearing top levels; projection `Module.heapFree_topDefFree`). The static fold carries the DIVERGED flag (4th `moduleInit` component): from the first exec-attempted statement (`g1ExecCandidate` + fold-refused assigns; `.unsupported` statements never diverge — `execStmt` refuses them) a static binding survives only heap-pure + ref-free (`g1HeapPure`/`g1DivGate`); everything else moves to the live view, same values, ONE heap. Executed statements may not rebind builtins or def/class/namedtuple names (`initBindable`, flush-checked, loud). VALUE TIERS with it (all differential, `seq_lab`): tuple/namedtuple SLICES (`seqSlice` — the `strSlice` validation verbatim, plain-tuple result; list slices stay loud), tuple REPETITION (`tupleRepeat`, budget-guarded), `sum(it[, start])` (`sumFold` IS `evalBinOp .add`; str start CPython's special TypeError; generator drain `moduleGenFree`-guarded like max/min), `tuple(it)`, and `range` as the IMMEDIATE `RVal.rangeV` (materialize-per-use under the FIXED `seqBudget` — budget refusals fuel-INDEPENDENT per the loudness doctrine; re-iteration exact; `==`/index/membership/unpack/boundary loud, dict-key via `keyRefusal` — ranges ARE hashable). EXTRACTOR: a MODULE-scope single-target `name = lambda …` is a ZERO-capture `NestedDef` (CPython symtable: module lambdas have no freevars — the flagged H7 rebinding fork DISSOLVED; the body reads globals through the live view at call time). THE CAPSTONE: the shipped padding loop RUNS — `pst` materializes with CPython's padded values (pinned per square), `K_MID`/`K_END` land, `Position.value()` runs on the shipped file (46/42/17-style CPython answers pinned), `directions`/`MATE_LOWER/UPPER` moved from static to live (spec pins updated); `g1_lab.read_m` FLIPPED from refusal to CPython's value (top-level `while` executes). Acceptance: `Examples/python/init_lab` (incl. the hand-built `insLoop` RuntimeError/rollback pin), `seq_lab`. Perf note: the three sunfish rotate proofs needed `set_option maxHeartbeats 1000000` (the resolution arms grew; the proofs are unchanged). PASS 6 (2026-08-11, docs/memory-model.md §the trace clock): TIME IS AN INPUT — `World.clock : List Int` is a finite trace of opaque integer readings; evaluating exactly `time.time()` (the pure `isClockCall`: attr test FIRST, receiver literally the name `time`, unshadowed — locals miss + statically poisoned + live-view miss — in a module passing the `moduleClockOk` census: benign `import time`, nothing else binding/storing `time`, no `global` anywhere) POPS the next reading; the empty trace is the LOUD fuel-independent underrun refusal; everything else on the clock name (bare value, `.sleep`, args, kwargs, shadowing) keeps the poisoned-binding refusal verbatim. Boundary: `callFunctionClock m f args clock fuel` (= `callFunction` at `clock := []` definitionally); batch jobs carry optional `"clock"`; cases.json rows carry `"clock": [ints] | "record"` — record-replay with the CPython side consuming the SAME integers it records (`time.time_ns() // 1000`; the model claims NOTHING about float readings — recorded restriction). The clock call is outside `Expr.heapFree` (attr ≠ `.get`), so `worldInv` is undisturbed (its attribute arm kills the fork by `rfl` on the first conjunct); `fuelMono` gained one `split`. Trace classes (Logic.lean): `ClockTrace.WallClock` (unconstrained — a `∀ tr` theorem consumes no axioms), `ClockTrace.Monotone` (nondecreasing, stated groundwork). Acceptance: `Examples/python/clock_lab` (happy pops, dead-clock consumption, armed `Stop` mid-loop at the exact reading, underrun at two fuels, shadowing/args edges, and `pure_sum_all_traces` — the first trace-quantified theorem, by SYMBOLIC EXECUTION with the trace free: a run that never pops never scrutinizes it. MEASURED TRAP: free-variable `rfl` does NOT scale (elaborator whnf shares no work — 4M heartbeats died on a ten-iteration run); search-sized `∀ tr` claims go through the CLOCK-ERASURE meta-theorem, BUILT in pass 7 (`LeanModels/Python/ClockErase.lean`, docs/memory-model.md §clock erasure design + as-built): `clockErase` — the 18-conjunct fuel induction mirroring `fuelMono` (`ClockErasedW`/`ClockErasedF` relate the empty-trace base run to the seeded family; decided/timed-out outcomes transport, refusals claim nothing — the underrun is the deliberate counterexample; `of_seed` closes fuel-free tails through the withClock simp normal form) — with boundary corollaries `callFunctionClock_ok`/`_exn`/`_timeout`, `CallsIn.clock_erased`, `CallsTo.clock_erased`: every decided empty-trace pin transports to `∀ tr` at the cost of one kernel run (`clock_lab.pure_sum_all_traces_transported` is the exemplar; the sunfish stepped-search `∀ tr` corollaries ride the spec-pole split, backlog).
4. Every Python example is a directory `Examples/python/<name>/` in the **three-file layout**: pure `<name>.py` (envelope only, no companion), hand-written `spec.lean` (checks + every theorem statement, each `:= by proofs`) and `proof.lean` (ALL real proofs, uniformly; namespace = its module path, e.g. `Examples.python.tri.proof`). The `proofs` tactic docstring in [Surface.lean](LeanModels/Python/Surface.lean) is the contract; statement duplication between the two files is by design and typechecked by the `:= by proofs` reference. A checks-only example has no `proof.lean` (`Examples/python/tut_01/`). Inline-mode exception — exactly one, showcased: `Examples/python/sum_to/` keeps theorems in `# lean[ … # ]` comment blocks inside the `.py`, spliced verbatim into the generated companion `Examples/python/sum_to/SumTo.lean`.
5. Statements use the typed surface (`LeanModels/Python/Surface.lean`): they never mention `Val`, fuel, or the AST — [docs/spec-surface.md](docs/spec-surface.md) is normative for shapes.
6. Goal states print back in surface notation (`LeanModels/Python/Delab.lean`) — the goal state is your interface; below `unfold CallsTo`/`obtain`, fuel and `callFunction` are visible by design.
7. [docs/DESIGN.md](docs/DESIGN.md) is the authoritative semantics/format contract; the semantics is differentially tested against CPython (`harness/diff_test.py`).
8. A SystemVerilog lane (`LeanModels/Sv/**`, `harness/sv/**`, and the SV example dirs `Examples/system-verilog/swap_nba/`, `Examples/system-verilog/counter/`, `Examples/system-verilog/race_blk/`, `Examples/system-verilog/adder/`, `Examples/system-verilog/xsel/`, `Examples/system-verilog/toggle/`) is mid-integration: not imported by `LeanModels.lean`, but its example `spec.lean`/`proof.lean` files build under the `Examples` glob (pulling `LeanModels/Sv/**` in transitively). It has its own contract ([docs/sv-design-m0.md](docs/sv-design-m0.md)) and harness (`harness/sv/diff_test.py`). Leave it alone unless it is your task.
9. Your deliverable: theorem statements in `spec.lean` (`:= by proofs`) with the real proofs in the sibling `proof.lean`, keeping `lake build` green from the repo root.
10. Read the docstrings of [Surface.lean](LeanModels/Python/Surface.lean) and [LoopTactic.lean](LeanModels/Python/LoopTactic.lean) before proving anything nontrivial — they are the tactic reference; this file only indexes them. Lookup tables (exact elaborations, tactic/type/CLI index): [docs/reference.md](docs/reference.md).
11. py_vcgen (v2 proof layer, in progress, additive): [LeanModels/Python/VC.lean](LeanModels/Python/VC.lean) — flow-aware total-correctness triples (`PyPost` arms `next`/`ret`/`brk`/`cont`/`err`, `PyTriple`/`PyStmtTriple` in fuel-threshold form) plus the structural rules — and [LeanModels/Python/VC2.lean](LeanModels/Python/VC2.lean) — the while rule (`PyStmtTriple.whileLoop`: invariant + measure, loop flow routed through the arms), the call rules and the `@[py_spec]` registry (arrow-form lemmas; local hypotheses interchangeable — recursion pattern proved in [LeanModels/Python/VCTests.lean](LeanModels/Python/VCTests.lean)), and the arrow⇄triple bridges (`callsTo_iff_triple`/`raises_iff_triple`), plus [LeanModels/Python/VCTactic.lean](LeanModels/Python/VCTactic.lean) — the `py_vcgen` walker tactic (layer 3): from a `==>`/`⇓`, `PyTriple`, or `∃ v, CallsTo … v ∧ Φ v` goal, per-loop `inv`/`dec` clauses (optional `exit<i>` for `break`-carrying loops; omitted clauses become delayed `inv<i>`/`dec<i>` goals) leave only named-atom math residuals. The module docstrings are the contract. The loop examples (tri, gcd, sum_to, rsa_inverse, nested_flow) are proved with it; goal-shape table below.

    Goal shape → `py_vcgen` (all in one call; residuals are clause math only):
    | straight-line `f(args) ==> v` | `py_vcgen [prog]`, no clauses |
    | one loop | `py_vcgen [prog] (inv := fun (<loop's Python names> : Int) => …) (dec := fun … => ….toNat)` |
    | nested loops / `break` / mid-loop `return` | i-th clause pair ↔ i-th `while` in source order, plus `(exit<i> := …)` when a break-carrying loop's continuation needs more than the invariant — [Examples/python/nested_flow/proof.lean](Examples/python/nested_flow/proof.lean) |
    | relational result | state `∃ v, CallsTo m "f" args v ∧ Φ v` and walk it — [Examples/python/rsa_inverse/proof.lean](Examples/python/rsa_inverse/proof.lean) |
    | call in the body | put the callee fact in scope as a local `CallsTo` hypothesis (rsa `inverse_core`), or `@[py_spec]` |

    Migration pitfalls: theorem binders shadowing the Python names go to a private core with renamed binders (`gcd_core`); a disjunctive invariant block must be ONE `abbrev` so the residual splitter keeps it whole (rsa `EgcdPhase`); orient invariant equations sum-to-variable (`lx * A + ly * B = a`), or they rewrite the symbolic environment; `omega` still consumes only unbranded comparisons — rebrand hypotheses (`have hn2 : (2:Int) ≤ n := hn`) or work in an `Int` core.

## The workflow

```
1. edit Examples/python/<f>/spec.lean + proof.lean   # statements ":= by proofs" in spec, real proofs in proof
2. lake build                                 # from repo root; must end "Build completed successfully"
```

Proof work is pure Lean — `spec.lean`/`proof.lean` are hand-written (no AUTOGENERATED header) and edited directly. The extractor enters the loop only when the `.py` itself changed: `python3 extractors/python/extract.py Examples/python/<f>/<f>.py` regenerates the envelope `<f>.json` next to the source (a block-less source regenerates the envelope and nothing else). Inline-mode exception (`Examples/python/sum_to/` only): theorems live in the `.py`'s `# lean[ … # ]` blocks — edit the `.py`, then re-extract to regenerate both the envelope and the companion `SumTo.lean`.

Before proving a new function's spec: add its concrete cases to `harness/cases.json` and run `python3 harness/diff_test.py` (must exit 0). The model is validated against CPython, not against your reading of the docs — this step has caught real spec bugs (see `Examples/python/gcd/spec.lean`). Non-int arguments (lists, tuples, strs, trees) are expressible as canonical typed JSON values — the runner's own `{"t":…,"v":…}` encoding — both in `harness/cases.json` rows and on the `leanmodels-run` command line (`valOfJson`, Main.lean; `from_typed`/`batch_job`, harness/diff_test.py — the harness runs ALL rows through one `leanmodels-run --batch` process; never spawn one runner per row, the per-row `lake` startup replay cost hours over 615 rows). Also state the concrete runs as `#py_check` lines.

Run `python3 tools/docs_check.py` before finishing doc edits: every path-marked code block in `docs/**`, `README.md`, and this file must match the referenced file verbatim (marker convention in the script's header; fix the doc, never the tree). The full check for any change is the triad `lake build && python3 tools/docs_check.py && python3 harness/diff_test.py`.

Every `spec.lean` opens with `#py_check` non-vacuity runs. Write them before the theorems.

## Judgment vocabulary

| Surface | Elaborates to | Meaning |
|---|---|---|
| `f(a, b) ==> v` | `CallsTo m "f" #[toVal a, toVal b] (toVal v)`, i.e. `∃ fuel, callFunction … fuel = .ok (toVal v)` | total: terminates and returns `v` |
| `f(a) ⇓ r` | the same `CallsTo`, hypothesis position, binding `r` | relational specs; prints back as `==>` |
| `f(a) ~~> v` | `PartialTo`: `∀ fuel r, callFunction … fuel = r → r = .timeout ∨ r = .ok (toVal v)` | strengthened partial: every run times out or returns exactly `v` — no exception, no `unsupported`, no other value |
| `f(a) ==>! e` | `Raises`: `∃ fuel, callFunction … fuel = .exn e` | terminates raising `e` |
| `#py_check f(a) = v` / `#py_check f(a) raises e` | `#guard` of one concrete run at fixed fuel **4096** | non-vacuity check |
| raw ∀-fuel form: `(h : callFunction f "f" #[.int a] fuel = .ok r) : r = .int …` | itself | the canonical `@[spec]` shape (conditional simp lemma) |

Elaboration notes: the identifier is both the loaded module constant and the function name; dotted `arith.mod(7, 0)` splits into module `arith`, function `"mod"`. Preconditions are ordinary named hypotheses (`(hn : 0 ≤ n)`). Binders use `Py*` brands (`PyInt` ≡ `Int`). Marshalling is `ToVal` (`toVal_int`, `toVal_nat`, … are simp lemmas). The spec RHS is mathematical Lean: Python `//` is `Int.fdiv`, `%` is `Int.fmod` — never "prettify" to `/` without a sign hypothesis ([docs/spec-surface.md](docs/spec-surface.md) §2).

## GOAL-SHAPE → TACTIC

| Goal shape | Tactic |
|---|---|
| `CallsTo _ _ _ _` (`==>`/`⇓`) or `==>! _`, loop-free body — straight-line or **one symbolic branch point** | `py_prove [f]` |
| loop-free body with **two sequential `if`s** (`py_prove`'s one-round split recipe fails — failure table below) | `refine ⟨32, ?_⟩`, `by_cases` each branch condition, `py_simp [callFunction, f, h1, h2] <;> grind` — see `Examples/python/ag_clamp01/proof.lean` |
| straight-line body through a **builtin `sorted` call** (`data = sorted(data)`, then `len`/index on the result) | `refine ⟨64, ?_⟩`, precompute the guard/index facts by `omega` (`hne : data ≠ []` decides the raise guard — simp normalizes `len == 0` all the way to `data = []`; `Int.fdiv_eq_ediv_of_nonneg` + `.toNat` bridges for `n//2` ranks; the `getElem?`-form access lemma `arrVal_getElem`), then one `py_simp [callFunction, f, asIntList_map_toVal, facts…]` per parity branch. The symbolic result keeps the `sortInts data` handle — bridge to Mathlib (`Pairwise`/`Perm`) via `sortInts_eq`; see `Examples/python/bench_statistics/proof.lean` |
| `CallsTo` goal, body has one `while` | `py_begin [f]`, then `py_loop` clauses (next row) |
| goal after `py_begin`: context has `hentry : ∀ F, callFunction … (F + 32) = …` containing a frozen `execWhile` | `py_loop (inv := fun (x y : Int) => …) (dec := fun (x y : Int) => …)`; binder names = the Python variable names; add `(state := […])` *before* `inv` when theorem binders shadow them |
| residual named-atom arithmetic (`hinv1…`, `hcont`, primed exit vars `i'`) | `omega` to pin values (`obtain rfl : i' = n + 1 := by omega`), then `grind`; pass library lemmas as needed (`grind [Int.gcd_zero_right, Int.natAbs_of_nonneg]`) |
| corollary of a total theorem: raw ∀-fuel `@[spec]` form, typed `⇓` form, `~~>` form, or value-rewritten `==>` | `py_corollary [tot]`; instantiate if needed (`py_corollary [fib_total n.toNat]`); extra value bridges after a comma (`py_corollary [midpoint_spec, Int.fdiv_eq_ediv_of_nonneg]`) |
| recursive function | induction on the **math argument** (`Nat.strongRecOn`), base cases `py_prove`; step: `py_lift ⟨f₁, h₁⟩ := ih … with [f]` per IH (thresholds are `callIn`-form since H1), then `refine ⟨f₁ + f₂ + 32, ?_⟩`, `unfold callFunction`, `rw [callIn.eq_2]`, `py_simp [f, facts…]`, then splice SEQUENTIALLY — `simp (disch := omega) only [h₁]; py_simp []; simp (disch := omega) only [h₂]` — the state THREADS through the first nested call, so the second call site stays stuck under its continuation until the first splice lands — see `Examples/python/fib/proof.lean` |
| loop provably never runs (guard false at entry) | constant-fuel witness: `exact CallsTo.intro 8 (by py_simp [callFunction, execWhile, f, h0])` — see `tri_neg_total` in `Examples/python/tri/proof.lean` |
| loop body **creates a variable** on its first iteration (CPython's `mid = (lo+hi)//2` idiom) — the env changes shape mid-loop, so `py_vcgen`/`py_loop` cannot align the state | hand-instantiate `execWhile_total_of_invariant` with a state carrying an `Option`/`Bool` slot for the not-yet-created variable (env-shape selector in the `toEnv` map), obligations per shape via `py_threshold`/`py_simp`, then splice back into `callFunction` with `execWhile_at_least` + `simp (disch := omega) only [hloop]` (or `py_begin`'s `hentry`) — see `Examples/python/bench_bisect/proof.lean` (both cold-prover runs hit this; docs/benchmark.md fix F-2) |
| threshold obligation `∃ f₀, ∀ F, f₀ ≤ F → <straight-line run> = .ok v` (hand-rolled loop lemmas) | `py_threshold 32 [facts…]` |

The proof to imitate for loops (real file, checked by `lake build` — the example proofs go through `py_vcgen`, item 11):

```lean
-- Examples/python/tri/proof.lean (three-file layout; statement re-stated in Examples/python/tri/spec.lean)
theorem tri_total (n : PyInt) (hn : 0 ≤ n) : tri(n) ==> n * (n + 1) / 2 := by
  py_vcgen [tri]
    (inv := fun (total i : Int) => 0 ≤ i ∧ i ≤ n + 1 ∧ 2 * total = i * (i - 1))
    (dec := fun (total i : Int) => (n + 1 - i).toNat)
  case ret =>
    obtain rfl : i' = n + 1 := by omega
    grind
  all_goals grind
```

With shadowed binders (`gcd` mutates `a`, `b`; the invariant needs the *initial* values, so they get renamed core binders):

```lean
-- Examples/python/gcd/proof.lean (three-file layout; statement re-stated in Examples/python/gcd/spec.lean)
private theorem gcd_core (A B : PyInt) (hA : 0 ≤ A) (hB : 0 ≤ B) :
    gcd(A, B) ==> Int.gcd A B := by
  py_vcgen [gcd]
    (inv := fun (a b : Int) => 0 ≤ a ∧ 0 ≤ b ∧ Int.gcd a b = Int.gcd A B)
    (dec := fun (a b : Int) => b.toNat)
  · exact ⟨a, ⟨rfl, hx⟩, hcore.1, by rw [← hcore.2.2, hx, Int.gcd_zero_right]⟩
  · exact Int.fmod_nonneg hinv1 hinv2
  · rw [gcd_fmod_step hinv1 hinv2]; exact hinv3
  · have := Int.fmod_lt_of_pos a (show (0:Int) < b by omega)
    have := Int.fmod_nonneg hinv1 hinv2
    omega
  · grind [Int.gcd_zero_right, Int.natAbs_of_nonneg]
```

Residual-goal order after `py_vcgen`: walk order — init, then per loop exit/preservation/decrease, then the `return` forks; any unclosed obligation is appended, never dropped (`py_loop` order: exit algebra, preservation, decrease, initial invariant).

## Failure modes

All error strings below were reproduced on the current tree. Walkthroughs with full goal-state context: [docs/tutorial/06-when-proofs-fail.md](docs/tutorial/06-when-proofs-fail.md).

| Symptom | Cause | Fix |
|---|---|---|
| `omega could not prove the goal` although the needed hypothesis is right there, and a binder is `PyInt`-typed | `omega` ingests a comparison only when its head type is literally `Int`/`Nat`; one headed at the `PyInt` brand — hypothesis **or goal** — is skipped wholesale (the brand is reducible; `omega` doesn't unfold it) | work through `py_begin` — it restates every `Py*`-branded hypothesis at the unbranded type. In hand proofs restate with an `Int`-typed term as the comparison's **left** operand (`have hx' : (0 : Int) ≥ x := hx` for `hx : x ≤ 0`) — the relation's type comes from the leftmost intrinsically-typed operand, and ascribing the branded variable (`(x : Int) ≤ 0`, `x ≤ (0 : Int)`) does NOT unbrand (reproduced in [tutorial 06, mode 5](docs/tutorial/06-when-proofs-fail.md#5-omega-ignores-a-pyint-typed-hypothesis)). A brand-headed *goal* comparison (spec-side `max`/`min` over `Py*` binders, `by_cases` conditions) can't be restated — close with `grind`, which matches up to reducible unfolding ([tutorial 06, mode 5](docs/tutorial/06-when-proofs-fail.md#5-omega-ignores-a-pyint-typed-hypothesis)) |
| `py_prove` leaves the un-split branch nest although the body is loop-free — it has **two sequential `if`s** | the branch alternative is one `split <;> py_simp <;> omega` round; on the fall-through arm `py_simp`'s full simp set rewrites the second surviving `ite` into a disjunction `split` cannot attack (`Tactic `split` failed: Could not split an `if` or `match` expression in the goal`) | decide the branches up front and hand them to symbolic execution: `by_cases h1 : x < 0 <;> by_cases h2 : 1 < x <;> py_simp [callFunction, f, h1, h2] <;> grind` (`Examples/python/ag_clamp01/proof.lean`; [tutorial 06, mode 7](docs/tutorial/06-when-proofs-fail.md#7-py_prove-on-two-sequential-ifs--the-branch-recipe-runs-out)) |
| `no proof named <decl> in Examples.<name>.proof — add it to proof.lean` on a spec-side `:= by proofs` | the statement has no same-name twin in the sibling `proof.lean` — not written yet, misnamed, or declared outside the `namespace Examples.<name>.proof` wrapper | write/rename the theorem in `proof.lean` under the exact spec-side name, inside the namespace; every real proof lives there |
| `:= by proofs` fails although the twin exists (the `first \| exact … \| apply … <;> assumption` alternatives run out) | the two statements have drifted — different binders, hypotheses, or value expression (the duplication is typechecked by exactly this reference) | make the spec and proof statements identical again; do not "fix" it by proving in the spec file |
| ``proofs: current module `<M>` is not an `<example>.spec` module`` | `:= by proofs` used outside a three-file `spec.lean` (the tactic pairs `….spec` with its sibling `….proof`) | only spec modules use `:= by proofs`; elsewhere write the real proof |
| `grind` fails on a goal whose invariant facts it should use | `grind`'s e-matching instantiates from **atomic** facts, not conjunctions | state the invariant as a top-level `∧`-chain — `py_loop` splits it into `hinv1`, `hinv2`, … for you; never re-conjoin them (no `⟨hinv1, hinv2⟩` repackaging before `grind`) |
| ``py_loop: no `hentry` hypothesis in context — run `py_begin [<prog>]` first`` | `py_loop` consumes the entry lemma `py_begin` produces | run `py_begin [f]` first |
| ``py_loop: loop variable `k` is not in the loop environment [n, s] — when the Python variable names are shadowed by ambient binders, name them with `(state := [...])` `` | `inv` binder names must *be* the Python variable names unless mapped | `py_loop (state := [s, n]) (inv := fun (s k : Int) => …) …` — `state` entries are matched **by name** (any order) against the loop environment; the i-th entry pairs with the i-th lambda binder, freeing binder names |
| `py_loop: could not derive the loop-test value / continuation condition / body's logical step` | test/body outside the v1 recipe (non-`Int` loop variables, `break`/`continue`/`return` in the body, two loops) — or a Miller pattern got destroyed: unification reads `tv`/`Cont`/`step` off goals containing metavariables, which surviving `ite`s, destructured states, and full-simp-set rewrites (e.g. moving `!` across `(b == 0) = !?Cont s`) all break | keep loops in the v1 shape ([LoopTactic.lean](LeanModels/Python/LoopTactic.lean) docstring); in hand-rolled obligations: keep the state variable whole (`intro s`, never destructure), collapse test branches with `ite_ok_bool`, and use `simp only [truthy]`, not `py_simp`, on the truthiness goal |
| ``py_begin: goal is not a `==>`/`⇓` (CallsTo) statement`` | `py_begin` only opens total-correctness goals | state the theorem with `==>`/`⇓`; get `~~>` and `@[spec]` forms afterwards via `py_corollary` |
| `py_prove` leaves a huge goal full of `execWhile` and AST literals | the body has a loop; `py_prove` is for loop-free bodies only | switch to `py_begin [f]` + `py_loop` |
| `#py_check` fails: ``Expression callFunction … 4096 == Res.ok … did not evaluate to `true` `` | wrong expected value — or recursion deeper than the fixed fuel 4096 (fuel is a *depth* bound) | fix the expectation; for deeper runs use a raw `#guard` with bigger fuel (concrete runs cost time proportional to steps, not fuel — generosity is free) |
| `warning: Left-hand side of simp theorem has a variable as head symbol …` on an `@[spec]` theorem | the raw ∀-fuel shape is a conditional simp lemma with variable head | prefix with `set_option warning.simp.varHead false in` (house style, every example file) |
| `simp` won't apply a threshold hypothesis `h : ∀ F, f₀ ≤ F → execWhile … = .ok p` | the loop lemma was applied at metavariable spans, so `simp` cannot index `h` | conditional `rw [h]` and discharge `f₀ ≤ F` by `omega` (`execWhile_at_least` docstring) |
| `simp only [prog] at h; py_simp at h` fails with "no progress" | two-step unfold/normalize breaks on hypotheses needing no cast normalization | fuse them: `py_simp [prog] at h` (what `py_lift` does) |
| a `first \| exact … (by …)` alternative "succeeds" then the proof is broken | `exact … (by …)` **commits** inside `first` even when the nested block fails (recovered with `sorry`, merely logged) | when writing tactics: put all-tactic attempts first, guarded by `done` (`py_prove` docstring) |
| `Env.lookup`/`Env.set` dot notation fails to resolve | `Env` is an abbrev of `List (String × Val)`; dot notation lands in the `List` namespace | use the full names |
| a fuel-arithmetic mess (`max f₁ f₂ + 3` offsets) | you are hand-counting interpreter steps | never do this: use threshold forms (`py_lift`, `CallsTo.at_least`, `execWhile_at_least`) + `simp (disch := omega) only [h]`, and generous slack constants (`⟨f₁ + f₂ + 32, ?_⟩` — any slack works) |
| `unexpected token '#guard'; expected 'lemma'` (or the `#py_check`/`#print axioms` analogue) right after a `/-- … -/` comment | doc comments attach to declarations only — commands cannot carry them, and the error never mentions the doc comment (both 2026-07-31 cold provers lost their only failed compile here) | use a `/-! … -/` section comment before command blocks — the pattern every `spec.lean` uses |
| the `sorted`-row helper lemmas (`arrVal_getElem`, `sorted_getD`, `getD_eq_getElem`) grep to nothing under `LeanModels/` | they are example-local, re-proved per proof file — promotion to a shared list-spec module is pending (docs/benchmark.md fix F-6) | copy them from `Examples/python/bench_statistics/proof.lean` (or `bench_bisect/proof.lean`) into your file |

Statement discipline when re-proving: the statements in the example files and [docs/spec-surface.md](docs/spec-surface.md) are **normative — reproduce them exactly**. Do not add hypotheses to make a proof pass; a genuinely needed hypothesis is a *semantics finding* — take it through the differential harness first (that is how `gcd`'s sign hypotheses were justified). Do not delete a hypothesis that turns out unneeded on this toolchain; keep it and record the fact (see `midpoint_nonneg` in `Examples/python/midpoint/spec.lean`).

Why `~~>` is the only admissible partial-correctness form: the naive "if it returns `.ok` then `v`" is **vacuously provable for every `v`** whenever the callee raises or diverges — a reward-hackable objective for an AI prover. `PartialTo` is falsifiable: it is inconsistent with `==>!` and with `unsupported` (`PartialTo.not_raises`, `PartialTo.iff_obs`). Totality subsumes it (`CallsTo.partialTo`, via fuel monotonicity), so prove `==>` and get `~~>` free via `py_corollary`.

## House rules

- **Induction on math variables, never on fuel.** `fib_total` inducts on `k : Nat` with `Nat.strongRecOn`; fuel induction lives only inside the framework (`fuelMono`, Obs.lean).
- **Threshold form for every spliced run**: `py_lift` / `CallsTo.at_least` / `execWhile_at_least`, side conditions by `omega`. Generous slack constants everywhere: `py_prove` and `py_threshold` use 32, `#py_check` uses 4096, witnesses use `… + 32`.
- **Typed surface in statements**: `Py*` binders, mathematical RHS, no `Val`/fuel/AST. The raw ∀-fuel form appears only in `@[spec]` corollaries, proved by `py_corollary`.
- **`@[spec]` only on Hoare-triple/simp shapes** (the raw ∀-fuel form, the `⇓`-relational form). The ∃-fuel arrows (`==>`, `~~>`) are *not* `@[spec]` (recorded in `Examples/python/add/spec.lean`). `@[spec]` is core Lean's mvcgen attribute: there is no `simp [spec]` set — cite lemmas by name.
- **Non-vacuity first**: `#py_check` block before any theorem; `unsupported`-outcome checks stay raw `#guard … matches` (no surface form — deliberate).
- **`#print axioms` of every `@[spec]` theorem** must show only `[propext, Classical.choice, Quot.sound]`.
- **`py_simp` freezes recursion points**: `callFunction`/`execWhile` are not in its simp set; unfold one step with `rw [callFunction.eq_2]` / `rw [execWhile.eq_2]`, pass them explicitly only when full unfolding is safe. Program literals must be passed explicitly (`py_simp [tri]`).
- **Kernel-reducibility is a tier constraint** (the mergeSort trap): any function the interpreter's helpers compute with must be STRUCTURAL recursion — core's `List.mergeSort` is well-founded recursion, the kernel does not reduce it, and `#py_check`/`py_check` (kernel `rfl`) and `py_vcgen`'s captured runs would all break. That is why `sorted` is `sortInts` (insertion sort) with the Mathlib `insertionSort` bridge living proof-side (`sortInts_eq`, bench_statistics). `sortInts`/`insertLe` also stay OUT of `py_simp`/`interpUnfolds` so symbolic goals keep the compact `sortInts data` handle.

## Never

- Never `sorry`, `admit`, or `native_decide` — anywhere, ever.
- Never edit generated files: `*.json` envelopes anywhere and the inline-mode companion `Examples/python/sum_to/SumTo.lean` (header says `AUTOGENERATED … DO NOT EDIT`). Edit the `.py`, re-extract. (`spec.lean`/`proof.lean` carry no AUTOGENERATED header and are edited directly.)
- Never weaken, strengthen, or "simplify" a recorded theorem statement to make a proof pass.
- Never add `"expect": "unsupported"` whitelist entries to `harness/cases.json` to silence a mismatch — whitelists document known tier gaps, nothing else. A non-whitelisted mismatch is an interpreter bug: report it, don't paper over it.
- Never state partial correctness as bare "if `.ok` then `v`" — use `~~>` (see above).
- Never leave `lake build` red, and never `git commit`.
- Never touch the SV lane's files (or `lakefile.toml` / `LeanModels.lean` / `lean-toolchain`) as a side effect of Python-lane work.
