{
 "schema": "declared-divergences-1",
 "tier": "C",
 "language_version": "C23 (N3220)",
 "note": "family-architecture.md \u00a75.0a. A row here is a DEBT, never a verdict. This file is opened by 2026-08-24-c-22, the landing in which `lake exe c-torture-run` produced the C tier's first non-zero `failed` count (2 of 300). A `failed` row on the scoreboard is a test that RAN and reached abort(); it is the only class of scoreboard row that can convict the model, so every one of them is listed here with its diagnosis or with the explicit absence of one.",
 "rows": [
  {
   "id": "c-div-1",
   "kind": "oracle-shape",
   "site": "gcc.c-torture/execute/20021127-1.c at pin 9e54d865. Scoreboard verdict: failed (reached abort()).",
   "oracle": "The test declares `long long llabs (long long);`, calls `llabs(-1)`, and DEFINES `long long llabs (long long b) { abort (); }`. It passes only if the implementation recognises `llabs` as a builtin and folds the call without calling the definition in the file. Reaching the definition is the failure signal the test was written to produce.",
   "model": "The model does the ordinary thing: `Program.find? \"llabs\"` finds the definition in the translation unit and calls it, so the call reaches `abort()`. THE MODEL IS BEHAVING CORRECTLY -- there is no C rule it is getting wrong. What it lacks is builtin RECOGNITION, which is a compiler optimisation and not a semantics.",
   "inherited_from": null,
   "declared": "2026-08-24",
   "retirement_condition": "Either the tier models the `<stdlib.h>` builtins by name (widening the libc slice, which retires it as a pass), or the scoreboard classes builtin-recognition tests out of the denominator with a NAMED state. The second is a scoreboard decision and is NOT taken unilaterally: silently excluding tests a model cannot pass is how a conformance number stops meaning anything.",
   "guards": [
    "docs/c-torture-pin.json lists 20021127-1.c with its sha256, so the test cannot change under the pin without the fetch refusing"
   ],
   "why_not_a_refusal": "It is not a refusal because nothing was out of tier: every construct in the test is modelled, the call ran, and the program reached abort() exactly as a conforming implementation WITHOUT builtin folding would. The oracle and the model disagree about what the program means, and the oracle is testing the compiler rather than the language."
  },
  {
   "id": "c-div-2",
   "kind": "retired-diagnosed",
   "site": "gcc.c-torture/execute/20010224-1.c at pin 9e54d865. Scoreboard verdict: failed (reached abort()).",
   "oracle": "`int16_t bndpsd[6]` is filled by `ba_compute_psd(0)`, which reads `masktab[0] == 1`, sets `bndpsd[1] = psd[0] = 50`, then accumulates `psd[1..3]` through `logadd(int16_t*, int16_t*)`. main() requires `bndpsd[1] == 140`; 50+40+30+20 = 140.",
   "model": "DIAGNOSED AND FIXED, 2026-08-24, within the one-landing deadline the row set itself. The model was RIGHT. clang's JSON dumper puts a PARTIALLY initialised array's elements under `array_filler` -- with the implicit filler prepended -- and leaves `inner` EMPTY, so `extractors/c/extract.py`'s `e_InitListExpr`, which read only `inner`, emitted `\"inits\": []` for `int16_t masktab[6] = {1,2,3,4,5}`. The tier then correctly zero-filled all six elements per \u00a76.7.11p21 and computed 0 where the test wanted 140. The envelope was a well-formed description of a DIFFERENT program.",
   "inherited_from": null,
   "declared": "2026-08-24",
   "retirement_condition": "RETIRED. `e_InitListExpr` reads `array_filler` when `inner` is empty and drops the `ImplicitValueInitExpr` filler, because \u00a76.7.11p21's zero-fill is the model's job and carrying clang's would be a second answer to one question. Verified end to end: masktab[0]=1, masktab[5]=0 (the zero-fill still correct), psd[3]=20, bndpsd[1]=140.",
   "guards": [
    "docs/c-torture-pin.json lists 20010224-1.c with its sha256",
    "the scoreboard's `failed` count is the live guard: it is 2, and any landing that changes it must account for both rows",
    "the scoreboard's `failed` count: this test leaves `failed` for `passed`, and the count drops 2 -> 1"
   ],
   "why_not_a_refusal": "It never was a divergence in the SEMANTICS -- and that is the reason the row was opened before it was understood. Had it been left as a number, `failed 2` would have read as two model defects, and the real defect was in the instrument that feeds the model."
  }
 ]
}
