{
  "tier": "python",
  "schema": "declared-divergences-1",
  "probe": "harness/pyc_divergence_probe.py",
  "note": "family-architecture.md 5.0a. DATA per-tier here; CHECKER is the shared harness/divergence_register.py (founded by this tier); PROBE is per-tier and named above. pyc-div-1 remains: its probe can run only the ORACLE side, because the divergence is not reachable by an in-tier program. pyc-div-2 (the genexp cursor built at first resume instead of at construction) RETIRED on 2026-08-24 at 2026-08-24-pycomplete-20, by its own condition and not by assertion: the cursor moved to construction time and dict_lab::genexp_bound_then_grow flips to MATCH. Its guard failing is what announced the retirement -- a register that only ever grows is a list, and this one shrank on evidence.",
  "rows": [
    {
      "id": "pyc-div-1",
      "kind": "semantic",
      "site": "stepIterAt (LeanModels/Python/Monadic/Eval.lean) on a dict-cursor frame -- GenFrame.iterDict or GenFrame.enumDict -- after its size guard has raised. Spelling: `it = iter(d)` / `d[k] = v` / `next(it)` raises / any further `next(it, x)`.",
      "oracle": "CPython 3.9.19 POISONS the iterator. dictiter_iternextkey sets di_used = -1 and its own comment calls the state sticky, so every later `next` -- with a default or without -- re-raises RuntimeError: dictionary changed size during iteration. MEASURED by the probe on every run. The tier's OTHER dict-iteration RuntimeError is deliberately not covered: after 'dictionary keys changed during iteration' CPython clears di_dict and the iterator merely dies, which the model already reproduces.",
      "model": "stepIterAt's tryCatch rewrites the object to `.generator qname locals [] .closed` and re-raises, so a later `next(it, x)` answers `x` and a bare `next(it)` raises StopIteration. Dead and poisoned are different states and this tier has only one of them. MEASURED BY RUNNING IT since 2026-08-24-pycomplete-23: the probe executes the witness program under CPython and under tools/leanpy and compares. Until then this field was a reading of the code, pinned at the absence of a poisoned GenStatus constructor -- honest, and weaker than a run.",
      "inherited_from": "enumDict, docs/backlog/python-completeness.md 2026-08-23-pycomplete-14 (inch 3c-i-c)",
      "inherited_note": "NOT originated here. iterDict (2026-08-24-pycomplete-18) reuses stepIterAt's close-on-exception path and adds no new decision; enumDict is where the path was chosen, and it was chosen without this being measured. The upstream owner is this same lane, which under 5.0a removes the excuse and not the citation.",
      "declared": "2026-08-24",
      "retirement_condition": "GenStatus gains a POISONED state that stepIterAt sets, instead of `.closed`, when the exception came from a dict-cursor frame's guard; and the witness program in pyc_div_1_still_divergent stops diverging -- both sides re-raising. The guard RUNS that exact program on both sides, so the row retires by the behaviour changing and cannot be closed by assertion. Fully exercisable as of 2026-08-24-pycomplete-23.",
      "guards": [
        "pyc_div_1_still_divergent",
        "pyc_div_1_has_not_widened"
      ],
      "must_not_move": "exc_lab::gen_closes must stay MATCH through any fix. Closing on exception is CORRECT for a user generator -- CPython closes those too -- and only the C-level cursor frames change. A fix that flips gen_closes has fixed the wrong thing.",
      "was_blocked_on": "exc_lab::except_builtin, RESOLVED 2026-08-24 (2026-08-24-pycomplete-22). The witness needs `except RuntimeError:` to survive the first raise, and the tier's admitted handler shape named user classes only; builtin handler classes are now admitted and the refusal left the whitelist. Recorded rather than deleted, because the row's own probe carried the claim 'THE DIVERGENCE IS NOT REACHABLE BY AN IN-TIER PROGRAM' for a day and a half, and a blocker that silently disappears leaves an instrument still describing the world it was written in."
    }
  ]
}
