# Quality audit — 2026-08-23 (coordinator)

Six finder lenses (the measured defect families: expectation provenance, guards-that-cannot-fail, silent absence, the identifier law in instruments, checkable comment claims, stated-vs-actual surface) swept the tree at `ae90d43`; 72 raw findings, every medium+high adversarially re-read by an independent verifier whose default stance was refutation. **56 confirmed** (11 high / 35 medium / 10 low), 8 refuted. Verifier corrections to findings are folded in; severities below are the verifier's, not the finder's.

Each row is owed a fix or an explicit won't-fix note in the owning lane's backlog. Quotes and full verdicts: this file records claim+fix; the audit's full JSON lives with the coordinator.

## python (14)

- **HIGH** [provenance] `Examples/python/bench_bisect/spec.lean:35` — The stated reason for skipping the live CPython oracle is false: `Main.lean:49` documents `args` elements as "either plain JSON integers or canonical typed JSON values" and `harness/cases.json` already carries 15 rows with `{"t": "list", ...}` arguments. And the cited fallback provenance covers only 4 of the 10 `#py_check` rows — bench_bisect.py's authenticity block lists bisect_left([1,2,2,3],2), bisect_right([1,2,2,3],2), bisect_left([10,20,30],25,0,3), bisect_left([],5); lines 56, 59, 60, 61, 62, 63 are not in it.
  - fix: Add bisect_left/bisect_right rows with typed list args to `harness/cases.json` so CPython adjudicates, and delete the stale 'ints only' gap note.
- **HIGH** [provenance] `Examples/python/sunfish/pins_genmoves.lean:240` — Thirteen boards' expected move lists are declared to be "CPython's own answer, in CPython's order", but `gen_moves` appears in ZERO rows of `harness/cases.json` (the live CPython differential), no generator script exists anywhere in the tree, and the same hand-typed 20-move literal is reused as the expectation for BOTH the interpreter run (line 41) and the `Ref` enumeration (line 248) — one transcribed column adjudicating two Lean implementations.
  - fix: Add `gen_moves` rows to `harness/cases.json` (typed list/tuple args are already supported) or commit the CPython printer that emitted these lists, and regenerate the literals mechanically from it.
- **MEDIUM** [coverage] `docs/completeness.md:63` — These are "current" counts fully derivable from harness/refusal_census.py's own W table and already wrong: parsing the `w(...)` calls gives 113 witnesses, 81 MATCH, 32 REFUSE (27 stmt + 27 expr + 24 dict + 4 const + 31 op). Line 68's "sixteen `dict.*` rows" is likewise 24.
  - fix: Have refusal_census.py emit the three totals (and the per-sort counts) into a generated block that docs_check.py pins, so the prose cannot drift from the W table.
- **MEDIUM** [coverage] `LeanModels/Python/Monadic.lean:11` — The stated surface says the trunk interpreter is authoritative and the rebuild is only build-checked, but the actual surface is the reverse: Main.lean routes every runner entry point through the rebuild (Main.lean:395 `Monadic.runScriptClockMono`, :543 `let run := Monadic.callInMono m fuel`, :633, :724), and docs/backlog/architecture.md 2026-08-23-architecture-13 records the two-model window CLOSED with Monadic as "THE INTERPRETER".
  - fix: Rewrite the Monadic.lean and Main.lean:4-13 headers to state that Monadic is the sole runner interpreter, and record explicitly that the trunk retains the theorem corpus while carrying no differential evidence.
- **MEDIUM** [docdrift] `harness/refusal_census.py:871` — The census prints its ROW count as a PRODUCTION count. The docstring at :11-14 enumerates the grammar as 25+27+13+4+10+2+4 = 85 productions, and the header at :864 says 'one witness per CPython 3.9 `ast` production', but the `W` table holds 113 rows: 24 `dict.*` rows (383-555) that are not `ast` productions at all, plus 4 extra per-production variants (`stmt.AnnAssign-local`, `stmt.AnnAssign-novalue`, `op.Pow-negative`, `op.RShift-budget`).
  - fix: Tag each witness with `kind="production"|"battery"|"variant"`, print production coverage over distinct `<sort>.<Production>` keys only, and report the dict battery on its own line.
- **MEDIUM** [docdrift] `LeanModels/Python/PayloadBlind.lean:21` — The cited range is stale by ~320-360 lines: the interpreter's mutual block is `LeanModels/Python/Semantics.lean:4549`-`6568`. Line 4232 sits inside `Expr.genAllocFreeList` (the genAllocFree helper mutual at 4189-4239) and 6207 is mid-`execWhile`. The same stale measurement drives :2694 ("1047 of its 1976 lines") — 6207-4232+1 = 1976 exactly, while the real block is 2020 lines and `evalExpr` (4555-5632) is 1078. `LeanModels/Python/PayloadBlind.lean:1636` repeats the range.
  - fix: Re-cite the block as `Semantics.lean:4549-6568` (2020 lines) at :21, :1636 and :2694, recompute `evalExpr`'s share (1078), and add a tripwire that fails if the block's first/last `def` moves.
- **MEDIUM** [absence] `harness/class_census.py:788` — Library-population rows that failed to parse or extract are filtered out with no print and no record — asymmetric with the seed population sixty lines above, which at least prints `ERROR %-24s %s` (lines 727-729) before filtering. Neither population's failures reach the emitted JSON (line 793-794).
  - fix: Print library errors the way seed errors are printed, and record `{"errors": [...], "attempted": N}` for both populations in the JSON so the denominator is auditable.
- **MEDIUM** [absence] `harness/script_corpus.py:131` — `default_oracle()` silently degrades python3.9 → python3 → `sys.executable`, swallowing the reason via `except OSError: continue`. Its own docstring records that this exact substitution already happened once and that "Every 'MATCH' was therefore measured against the wrong reference". harness/leanpy_survey.py:330-336 is a byte-identical copy consumed by harness/refusal_census.py:100.
  - fix: Make the pin mandatory: refuse when `python3.9` is not on PATH unless `LEANPY_CPYTHON` explicitly names an override, and never fall back to `sys.executable`.
- **MEDIUM** [absence] `harness/qs_cut_census.py:56` — The shadow fold's exceptions are swallowed and the node is then silently excluded from the census (`if shadow is not None: REC.append(...)` at line 61), so a node the model cannot even evaluate is dropped rather than counted as a divergence.
  - fix: Count shadow-fold exceptions into a `crashed` bucket, print it beside the mismatch line, and make any non-zero crash count refuse the MODEL-REPRODUCES verdict; replace `or 1` with an explicit zero-corpus refusal.
- **MEDIUM** [provenance] `harness/qs_cut_census.py:20` — The census instrument behind the QS-cut / fail-low numbers imports the engine from `harness/lean-basecase/…`, a directory that does not exist in the repository (and is not gitignored — it was a scratch clone).
  - fix: Point `SF` at `Examples/python/sunfish` in this repo (or accept a `--corpus` argument), and record the census output as a committed JSON artifact the Lean file can cite.
- **MEDIUM** [provenance] `harness/refusal_census.py:107` — `docs/family-architecture.md` §5.3 holds this column up as the general shape — "the refusal census's expectation column — written from CPython's measured behaviour rather than the model's" — but the column holds VERDICTS, and 32 of the 113 witnesses record `REFUSE`, a state produced entirely by the model (verdict() returns REFUSE on `status == "unsupported"` before CPython's output is consulted at all, line 818-819).
  - fix: Store CPython's own observable beside each witness (stdout/exit/exception class) so REFUSE rows still carry an oracle column, or narrow §5.3's claim to the MATCH rows.
- **LOW** [coverage] `docs/completeness.md:155` — The refusal-surface "upper bound" (§4, "Counted, not sampled", line 151) is measured over the glob `LeanModels/Python/*.lean`, which excludes `LeanModels/Python/Monadic/` — the only interpreter any harness runs. The retired trunk holds 142 distinct `.unsupported "…"` literals (16 internal); Monadic/ holds 9, and spells its refusals `refuse` at 181 sites.
  - fix: Re-scope §4 to `LeanModels/Python/Monadic/**` and count the `refuse`/`preRefuse` spellings the rebuild actually uses, or state both surfaces with the state each was measured in.
- **LOW** [absence] `harness/refusal_census.py:80` — When the pinned 3.9 frontend is missing, the census prints a WARNING to stderr and proceeds to extract with whatever interpreter is running — and the `--json` artifact it then writes (line 1017-1020) records only `{"grammar": [{id, verdict, detail, recorded}]}`, with no frontend, no oracle, no drift count.
  - fix: Refuse (exit 2) when the pinned frontend is absent unless `LEANPY_FRONTEND` explicitly overrides, and stamp `frontend`, `oracle` (with its `-V` output and checked return code) and `drift` into the `--json` document.
- **LOW** [vacuous] `harness/script_corpus.py:209` — The exception-message half of the comparison is gated on the MODEL's own output shape: if the Lean side's stderr line contains no `": "`, the message is not compared at all. The side under test decides whether it gets checked. The class comparison also passes when BOTH sides parse to `None` (`exc_class` returns None for an unparseable last line and for empty stderr), so a CPython traceback whose final line is a message continuation paired with a silent model exit-1 compares equal.
  - fix: Count rows whose message comparison was skipped and print that count beside 'completed-and-matched'; treat both-sides-None exception classes as an instrument ERROR rather than agreement.

## qol (12)

- **HIGH** [identifier] `tools/laws.sh:104` — home_tokens caps a section token at TWO levels, so a law homed at §3.4.1 or §3.5.1 tokenises to `§3.4` / `§3.5`; tok_regex then appends the boundary `([^0-9.A-Za-z]|$)`, which excludes a following '.', so the truncated token can never match the three-level citation it came from — while matching every unrelated sibling that spells the two-level form.
  - fix: Extract the full dotted section (`§[0-9]+(\.[0-9]+)*[a-z]?`) as the token, and, if parent-section credit is wanted, emit the parent as a SEPARATE token whose boundary permits a following '.'.
- **HIGH** [identifier] `tools/substrate.sh:249` — adequacy_for decides STMT-67 with two INDEPENDENT unanchored BRE greps over the file's theorem lines: they need not match the same theorem, and `$a`/`$b` are raw names with no word boundary, so a name that is a prefix of the other satisfies both from one line.
  - fix: Require ONE theorem statement (accumulated across its continuation lines) to name both, matching each name with a boundary: `grep -E "(^|[^A-Za-z0-9_'])$a([^A-Za-z0-9_']|$)"` and likewise for `$b`, on the same accumulated statement.
- **HIGH** [identifier] `tools/substrate.sh:143` — REF_LOCAL is reported as 'locally-declared constructors' but the pattern matches ANY line beginning with `| unsupported` — including `cases`/`match` arms inside proofs and defs — the exact defect sites.sh's `declaring_types` documents and fixes ('a `| unsupported` MATCH ARM inside a later `def` is attributed to the last inductive seen'); and the trailing `[ \t(]` requires a character after the name, so a nullary constructor at end of line is missed.
  - fix: Reuse sites.sh's `declaring_types` block-tracking (open on `inductive|structure`, close on the next top-level keyword) and relax the trailing class to `([ \t(]|$)`.
- **HIGH** [identifier] `tools/analogues.sh:49` — These shape regexes are handed to awk via `awk -v RE="$re"`, which strips one backslash level before the string is used as a dynamic regex — the exact `awk -v` defect sites.sh already recorded — so `\(`becomes a group-open, `\[`/`\]` become a character class, and `\b` becomes a literal backspace.
  - fix: Pass the regex through a file or ARGV instead of `-v`, or double every backslash at the `shape_regex` site, and add a self-test asserting each named shape matches at least one known instance.
- **HIGH** [vacuous] `tools/a6-guard.sh:35` — The guard's only refusal path is unreachable whenever `lsof` is absent, fails, or is denied permission: stderr is discarded, `d` is empty, every candidate pid is `continue`d, `found` stays 0, and line 41 exits 0 — 'the tree may be rewritten'. The same holds if `pgrep` is missing (line 32, also `2>/dev/null`).
  - fix: Probe `command -v lsof && command -v pgrep` up front and `exit 1` (refuse) when either is missing or `lsof` exits nonzero; add a `--self-test` that drives the positive case through a mocked reader, and call the guard from triad.sh's rebase/merge preconditions.
- **MEDIUM** [coverage] `tools/triad.sh:497` — `example_fixture_is_reachable` decides a fixture's build relevance by a bare-basename fixed-string grep over prose as well as code — MEAS-13's own "a bare-word grep over prose is not a type census". `Examples/go/bitlen/guards.lean:10` mentions "`bitlen.go`" only inside a docstring sentence, and that mention is the sole hit that makes the fixture "reachable".
  - fix: Restrict the probe to non-comment text, or to declared inputs (lakefile `[[input_dir]]`, gate-corpus JSON paths), so a docstring cannot change a build target.
- **MEDIUM** [identifier] `tools/substrate.sh:142` — REF_CORE counts refusal SITES with a pattern that (a) hard-codes exactly one space between the channel and `(`, (b) never sees a site whose constructor application is split across lines, (c) uses `grep -c`, which counts LINES not occurrences, and (d) does not strip comments — while the documented pattern P_REFUSAL is parameterised on `<ctor>` and this one is hard-wired to `unsupported`.
  - fix: Reuse sites.sh's comment-stripping `code_hits` and its `[ \t]*` spacing, count occurrences with `grep -o | wc -l`, and drive the constructor from a parameter so the report matches P_REFUSAL.
- **MEDIUM** [identifier] `tools/substrate.sh:154` — STMT-19's UNCATCH column is decided by two unanchored, comment-blind greps: the first accepts any line containing 'theorem'/'example'/'lemma' as a substring (so prose, and `lemmas`/`examples`) that also contains `tryCatch`; the second accepts any `tryCatch` within two lines of the word `Halt` — which every tier built on `ExceptT ρ (StateT W Halt)` writes constantly.
  - fix: Strip Lean comments before matching (sites.sh's `code_hits` awk already does this in the same repo), and require the keyword and the catch token to be in the SAME declaration rather than merely the same line/two-line window.
- **MEDIUM** [absence] `tools/docs_check.py:81` — The default corpus (README.md, AGENTS.md, docs/**/*.md) is filtered by `is_file()`, so any of them going missing or being renamed silently shrinks the corpus; `(REPO / "docs").rglob("*.md")` on a nonexistent directory also yields nothing without error, and `main()` has no floor on `n_checked`.
  - fix: Refuse when any hardcoded default path is absent, and refuse when `n_checked == 0` ("zero marked blocks is an instrument fault, not a clean tree").
- **MEDIUM** [vacuous] `tools/ci.sh:17` — 34 of ci.sh's 39 steps go through `maybe`, which turns a MISSING gate file into SKIP rather than failure — so deleting or renaming `tools/docs_check.py`, `harness/sv/diff_test.py`, `harness/spice/*.py` etc. leaves CI green. Neither `harness/refusal_census.py` nor `harness/script_corpus.py` is invoked at all (`grep -c 'refusal_census\|script_corpus' tools/ci.sh` = 0), and no tool's `--self-test` runs in CI or in .github/workflows/ci.yml.
  - fix: Split `maybe` into 'optional because the SIMULATOR is absent' (SKIP) and 'the harness file itself is missing' (FAIL), and add a `self-tests` step running every `tools/*.sh --self-test` plus `harness/ada_round_trip.py --self-test`.
- **MEDIUM** [vacuous] `tools/triad.sh:848` — `axiom_ledger` is the only consumer of the build log's axiom lines and it has no failing path: it greps, echoes, and returns 0 whenever ANY axiom line exists — it never inspects the bracketed axiom list, so `[sorryAx]` and `[Lean.ofReduceBool]` print identically to `[propext, Classical.choice, Quot.sound]` and the tenure still prints TRIAD DONE ... gates green.
  - fix: Have `axiom_ledger` scan each line's axiom list and return nonzero (aborting the green verdict) on any axiom outside the declared standard set, naming the offending declaration.
- **LOW** [coverage] `tools/triad.sh:432` — `example_dir_tiers` is a hand-maintained Examples-dir → tier map whose comment asserts go has no model tier; `LeanModels/Go/` exists (1,129 lines across Sem/Spec/SpecAttr/Stmt/Value.lean) and both `Examples/go/rung1/guards.lean:1` and `Examples/go/bitlen/guards.lean:1` are `import LeanModels.Go`. The map's own header claims it "is not derivable", but for go it is derivable from those imports.
  - fix: Derive the map from the `import LeanModels.<Tier>` lines actually present under `Examples/<dir>/**/*.lean`, keeping the explicit case only for the genuinely ambiguous `mixed-signal` row.

## sv (5)

- **HIGH** [provenance] `docs/sv-corpus-coverage.md:351` — The artifact named as backing every table in the document is gitignored (`.gitignore` line: `harness/sv/conformance/census.json`) and absent; the second named artifact, `harness/sv/conformance/unlockable.txt`, described as holding 'the 11 ch-4/6/11 unlockable simulation tests, one corpus-relative path per line', is 0 bytes.
  - fix: Commit the census summary block (or a reduced per-chapter roll-up) and populate `unlockable.txt`, or mark both as regenerate-locally with the exact `extractors/sv/census.py` invocation and corpus revision.
- **MEDIUM** [vacuous] `LeanModels/Sv/Param.lean:997` — `PDesign.crossCheck` — documented at line 913 as 'the ingestion-time differential test against the frontend' — skips every comparison whose `resolved?`/`resolved_count` metadata is absent (same `| none =>` arms at 944 in checkLocals, 962 in checkDecls, and the `.ok _, none => []` arm in checkGenerates) and returns `[]`. It carries no count of comparisons actually made, so 'clean' and 'compared nothing' are the same value.
  - fix: Return a (messages, comparisons_made) pair and have `load_design_sv2` refuse when comparisons_made is 0 (or below a per-envelope floor), reporting VACUOUS rather than accepting.
- **MEDIUM** [provenance] `Examples/system-verilog/adder/proof.lean:275` — The stimuli these guards use were never run through a simulator: `harness/sv/cases.json`'s adder cases drive 1+2, 255+1, 170+85, 0+0, 127+127, while the guards below use a=5,b=3 and a=200,b=100; xsel's guards use a=0xAA/b=0x55 while the harness drives 0x0F/0xF0 and 0xA5/0x5A. The same sentence appears in counter/xsel/swap_nba/race_blk/toggle spec.lean:36 and proof.lean.
  - fix: Either add these stimuli as cases in `harness/sv/cases.json` so xrun/iverilog adjudicates them, or reword the claim to 'consistent with the LRM rules the harness verified' rather than 'reproduces the Xcelium-verified outcomes'.
- **MEDIUM** [provenance] `LeanModels/Sv/SelfCheck.lean:51` — The rendering battery's provenance citation resolves to nothing: `docs/sv-corpus-coverage.md` has sections (a)–(e), 'Extractor robustness notes' and 'Artifacts' — there is no §f — and the fixture ids the guards are labelled with (`Xcelium T01–T06`, `T07–T10`, `T11–T19`, `T20–T21`, `T29`) appear in no other file in the repo.
  - fix: Either add the §f fixture table (T01–T29 with the recorded Xcelium output) to `docs/sv-corpus-coverage.md`, or commit the .sv decks and regenerate these rows from a simulator run.
- **MEDIUM** [provenance] `LeanModels/Sv/Sem2.lean:24` — The whole sv-0.2 differential ledger (pin1 R02–R11, pin2a W01–W08/C01–C05, pin2b I01–I09, pin3 CYCLE rows, pin4 Z01–Z07/S01–S05) cites its oracle rows to `scratchpad/rows/pin1–pin4.sv`, which is not in the repository and not in git; `pin2a`/`pin2b` appear in no docs file, so the row identifiers the #guard comments key on have no register anywhere.
  - fix: Commit the pin decks under `Examples/system-verilog/sem2/probes/` (where t2_*.sv already live) and cite those paths, per family-architecture §7.1a 'Anything a lane must obey belongs in a git-tracked file'.

## c (5)

- **MEDIUM** [coverage] `LeanModels/C/C23/Value.lean:38` — The widths do not "come from" the profile — they are hand-transcribed integer literals in Lean (`def int_ : IntTy := ⟨true, 32⟩`, lines 75-82), and `c_profile_probe.py --check` gates a HOST against the JSON (probe_fact via clang `_Static_assert`) and never reads a Lean file. No tool in tools/ or harness/ cross-checks the Lean literals against docs/c-profile.json; grep for `c-profile` across tools/ returns nothing.
  - fix: Either derive IntTy's widths from the envelope's `profileId` at ingestion, or add a gate that parses the widths out of Value.lean and diffs them against docs/c-profile.json's facts (and say in the docstring which one is the source).
- **MEDIUM** [provenance] `Examples/c/sunfish/memory.lean:20` — The struct-layout table (Pos size 144 / score 120 / ep 128 / kp 132 / h 136, Move 12, kcctx 24) is cited to `docs/c-profile.json`, but that artifact carries 13 facts (CHAR_BIT, sizeof(int), endianness, …) and NOT ONE layout offset — no `Pos`, no `Move`, no `kcctx`, no 144/124/132/136 anywhere in the file; `harness/c_profile_probe.py` does not probe layout.
  - fix: Emit the `_Static_assert` layout probes as facts in `docs/c-profile.json` (both hosts) and derive `posSize`/`posScore`/`posEp`/`posH` from that artifact, or state plainly that these are unrecorded hand measurements.
- **LOW** [docdrift] `LeanModels/C/C23/Stmt.lean:343` — The headline is refuted by its own parenthetical and by the sentence after it: if only 48 of 50 carry `init` and 49 carry `cond`, then 2 and 1 sites do NOT carry all three, which is exactly the '3 sites' the next clause names. `docs/c-construct-census.json` confirms `control/ForStmt = 50`. The identical wording is propagated to `docs/c-semantics-design.md:601`.
  - fix: Rewrite as '50 sites; 47 carry all three clauses (48 `init`, 49 `cond`, 50 `inc`), so the omitted-clause arms are 3 sites, not a third of them' — and fix the same sentence in `docs/c-semantics-design.md:601`.
- **LOW** [absence] `harness/c_api_census.py:187` — `source` and `sha256` are hardcoded literals emitted as provenance, never computed from the tree actually read via `SRC` (line 12, whose default is a session-scratchpad path). Nothing verifies that the headers globbed at line 59 came from the tarball named here.
  - fix: Compute the digest (or refuse when a `--source-sha256` argument is not supplied and verified), and refuse when `api_universe` is empty rather than emitting `api_universe_size: 0` alongside all-empty module rows.
- **LOW** [absence] `harness/c_suite_census.py:138` — `source_facts()` returns an empty dict on any read failure, and `census()` does `row.update(source_facts(p))` (line 274), so the row simply lacks `needs_libc`, `hosted_headers`, `needs_cpp` — no error status, no marker.
  - fix: Set `row["status"] = "source-unreadable"` and record the OSError instead of returning `{}`, so the row leaves the `ok` set rather than joining the freestanding count.

## leantier (4)

- **HIGH** [identifier] `harness/lean_independent_check.py:60` — REFUSAL_MARKERS contains the bare token "unsupported", tested with an unanchored `m in low` substring over the WHOLE concatenated stdout+stderr of the lean4lean run, and that test runs BEFORE the DIVERGE fall-through (line 89), so any rejection message that mentions the word anywhere is reclassified as REFUSE.
  - fix: Match refusals against anchored checker-emitted phrases (e.g. `re.search(r"lean4lean does not support", ...)`, `"unsupported (declaration|construct)"`) rather than the bare word, and require the marker to appear on the same line as the checker's own refusal prefix.
- **MEDIUM** [identifier] `harness/lean_kernel_census.py:220` — Rule presence is decided by an unanchored `symbol in line` substring over C++ source (comments, forward declarations and call sites included), and the FIRST such line is published as `definition_line` — first-hit-per-file, the precedent sites.sh's `arms_of` explicitly refuses to repeat.
  - fix: Match the definition form with a boundary-anchored regex (e.g. `^[^/]*\b<symbol>\s*\(` at column 0 or after a return type) and refuse when the first hit is not a definition rather than publishing it as one.
- **LOW** [identifier] `harness/lean_rule_correspondence.py:203` — Constructor detection requires EXACTLY two leading whitespace characters before the bar (`^\s{2}\|`), so a constructor indented by four spaces, by a tab, or inside a nested namespace block matches nothing — and an empty constructor list is returned with no zero-row refusal, unlike every other input fault in the file. harness/lean_kernel_census.py:66 carries the identical pattern.
  - fix: Use `^\s+\|\s*` with the block's own base indentation, and raise CensusRefusal when an `inductive` block yields zero constructors.
- **LOW** [identifier] `harness/lean_rule_correspondence.py:251` — _sorry_stubs counts obligation lines with a raw regex over unstripped Lean source — no comment or string handling — which is precisely the defect harness/wasm_sorry_census.py was written to fix ('a textual grep counts `sorry` inside `--` comments ... an inflated ledger is the kind of wrong fact this repository does not ship').
  - fix: Import wasm_sorry_census.strip_lean (or call the census) instead of re.search on raw lines, and report the raw/live delta the way that census does.

## es (4)

- **HIGH** [absence] `harness/es_census.py:378` — This is the exact probe the family doc names as the law's founding exemplar — "a measurement of an ABSENT REPO" whose `[]`/`false`/`null` "reads as a finding" — and it is still unhardened: a missing `.github/workflows` yields `[]`, and `ignore_file_present` yields `false`, indistinguishable from a measured zero. Commit 28b9f5e ("a failed revision REFUSES instead of stamping null") hardened `rev()` at line 787 of this same file and left this sibling untouched, which is precisely the failure the doc predicts.
  - fix: Raise `Refusal` when `ignore` or `wf` is absent ("the esmeta surface is part of the result; census a CHECKOUT, not a fetched spec.html"), matching the refusal `rev()` already makes twelve lines below, and re-run to replace the recorded absence.
- **MEDIUM** [docdrift] `LeanModels/Es/Completion.lean:69` — Both ADOPTION NOTEs in this file (:16 for `SemM`, :69 for `RefusalCause`) assert a Core absence that Core contradicts: `LeanModels/Core/Outcome.lean:116` defines `inductive RefusalCause (π : Type)`, `:227` `SemMWith`, `:232` `SemM` — and `LeanModels/Go/Sem.lean` already consumes them, while `Completion.lean` imports only `LeanModels.Es.Value` and still defines its own `RefusalCause` at :118 and `SemM` at :238.
  - fix: Delete both ADOPTION NOTEs, import `LeanModels.Core.Outcome`, delete the local `RefusalCause`/`SemM`, and restate `es_never_undefined`/`es_never_orderDependent` against Core's type (as `LeanModels/Go/Spec.lean:150` already does).
- **MEDIUM** [identifier] `harness/es_lean_lint.py:45` — A doc comment that closes with code on the same line (`/-- doc -/ def f := 1`, legal and common Lean) never satisfies `endswith("-/")`, so j runs to the end of the file; `i = j + 1` then terminates the scan loop, and BAD_FOLLOWERS is a list that omits `set_option` — one of the three incidents the module's own docstring says it exists to gate.
  - fix: Track the `-/` terminator by scanning within the line (not `endswith`) and continue scanning after it on the same line; add `set_option` to BAD_FOLLOWERS with a fixture, since it is a recorded incident.
- **MEDIUM** [absence] `harness/es_lean_lint.py:79` — `check()` (lines 63-68) drops any path that is neither a directory nor a file with no message, and there is no floor on `len(files)`, so an empty file list prints "0 files clean" and returns 0.
  - fix: Refuse on any argument path that resolves to neither a file nor a directory, and refuse when zero files were collected.

## ada (4)

- **MEDIUM** [absence] `harness/ada_suite_census.py:688` — A missing or unparseable VERSION.A yields `(None, None)`, recorded verbatim as `"acats_version": null, "language_version": null` (lines 720-721); `SUITE_EDITION.get(major)` additionally returns None for any unlisted major. The sibling guard `manifest_check()` has the same shape — `if listed is None: return None` (line 635) when no ACATS*.LST is found.
  - fix: Refuse when VERSION.A is absent or its major is not in SUITE_EDITION, and refuse when no ACATS*.LST is found rather than returning None from the completeness guard.
- **MEDIUM** [vacuous] `Examples/ada/report/guards.lean:140` — The stated claim — 'Every marking carries a full CERR-grade span ... which need a line AND a position' — is not what the predicate tests. All-zero or absent positions (line=0, col=0, endLine=0, endCol=0) satisfy it, so the guard cannot detect the missing position it exists to pin; and `endCol ≥ col` is not a validity property of a multi-line span (a span from 10:40 to 12:5 is legal and would fail).
  - fix: Assert the positive property instead — every marking has `line > 0 && col > 0` and `(endLine > line || (endLine == line && endCol >= col))` — so absent positions fail and legitimate multi-line spans pass.
- **MEDIUM** [vacuous] `harness/ada_round_trip.py:331` — `bad` is set only by DIVERGE/ERROR; SKIP is explicitly exempt (docstring line 54: 'SKIP is reported and does not fail the gate'). Because `regenerate()` returns SKIP whenever a recorded source is not found under `--source-root` (default `"."`), a run pointed at the wrong root reports `round-trip: DIVERGE 0, ERROR 0, MATCH 0, SKIP N` and exits 0.
  - fix: Refuse (nonzero) when MATCH + DIVERGE is 0, or when the SKIP fraction exceeds a declared floor, so a wholly-skipped run is an instrument-level ERROR rather than a pass.
- **MEDIUM** [vacuous] `harness/ada_round_trip.py:357` — The self-test whose docstring says 'The gate must FAIL a tampered envelope, or it is decoration' never once exercises the round-trip. Three of its six cases expect `"SKIP"`, which `check()` returns because `verdict()` passes an EMPTY temp dir as source_root, so `regenerate()` bails at line 103 before any comparison; the other three return DIVERGE from the pre-checks at lines 140/146/158. No case ever reaches EDGE 2/3, the `markings`/`unsupported_count`/`profile_id` loop, or the `compilation_units payload differs` compare, and no case ever produces MATCH.
  - fix: Add self-test cases that stage a real source file under source_root so `regenerate()` runs, asserting MATCH on an untampered envelope and DIVERGE on one with a mutated `compilation_units` payload / `unsupported_count`.

## arch (3)

- **MEDIUM** [coverage] `docs/family-architecture.md:418` — A count derivable by one grep and already wrong: `grep -rl '/Users/ahle\|/home/thomas-ahle' docs/` returns three files, of which at least two are live records rather than discussion — docs/sv-corpus-coverage.md:5 (`/home/thomas-ahle/mox/...`) and docs/sv-construct-census.json:5 (`"corpus_path": "/Users/ahle/repos/mox/sv-conformance/sv-tests-2/tests"`), a committed census provenance field.
  - fix: Name both instances (and add a repo-wide private-path grep to tools/check.sh) so the redaction is complete rather than the one that was noticed.
- **MEDIUM** [coverage] `docs/family-architecture.md:346` — Both numbers are derivable and now stale, and the row carries no state stamp (MEAS-10). At the commit that wrote them (8f4fd65) LeanModels/Sv was exactly 8,166 lines with 93 theorems + 5 examples = 98; at HEAD it is 8,562 lines with 96 theorems + 10 examples = 106. Line 410 calls the count "SETTLED at 98".
  - fix: Stamp the row with the revision it was measured at, and add the two counts to tools/editions.sh or a --check mode so the registry drifts loudly.
- **LOW** [docdrift] `docs/family-architecture.md:3583` — Both halves are refuted by the cited file. `Examples/go/rung1/guards.lean:79-81` reads `def refusalOf (stmts : List Stmt) : Option (RefusalCause SpecRef) := … | .error (.unsupported c _ _) => some c` — a three-field pattern, not `.unsupported m`, and the return type names Core's `RefusalCause` (`LeanModels/Core/Outcome.lean:116`, reached via `import LeanModels.Go`). The same table's '11 lines / 3 files' is 11 lines across 5 files by the doc's own recommended grep (`.error (.unsupported`: Go/Spec.lean, Core/Outcome.lean, Monadic/Substrate.lean, Examples/go/bitlen/guards.lean, Examples/go/rung1/guards.lean).
  - fix: Re-quote `refusalOf`'s current body and either pick a genuinely non-naming destructurer as the convicting case or restate the point as 'reaches the shape without importing the module', and re-run the pattern grep for the table's row.

## go (3)

- **MEDIUM** [coverage] `LeanModels/Go/Stmt.lean:9` — The header (lines 6-9) declares rung 1 as "45 `go/ast` node kinds reaching 3,084 of the standard library's 5,419 files (56.9%)" and says this file carries that vocabulary. It does not: of the 17 kinds rung 1 added over rung 0, `SwitchStmt`, `CaseClause`, `IndexExpr`, `SliceExpr` and `ArrayType` have no `Stmt`/`Expr` constructor at all and land in `.unmodeled`, which refuses (line 481).
  - fix: Reword the header to say the file carries rung 1's INGESTER vocabulary and steps a named subset, and cite §G6's walker table for what is actually executed.
- **MEDIUM** [vacuous] `Examples/go/bitlen/guards.lean:142` — The section headed '## Non-vacuity — A differential row that cannot fail is decoration. Two things are checked here' checks nothing new: line 142 is byte-identical to line 117, and line 141 is strictly implied by it. The section's own text concedes the only real evidence — 'that a WRONG expectation actually breaks the build' — 'is not left in the file'.
  - fix: Replace the duplicated rows with a mechanized mutation check — e.g. a harness that perturbs one expected value and asserts the build goes red — or delete the section and cite the ledger entry as the only evidence, rather than staging two rows that look like a test.
- **MEDIUM** [provenance] `Examples/go/bitlen/guards.lean:81` — The exemplar the provenance law generalizes from ships no generator: `Examples/go/bitlen/bitlen.go` states "NOT compiled as part of this repository", has no `main`, no `printf`, and no `package main`; `harness/go/` contains only the construct census. Nothing in the tree can re-emit the 35 rows.
  - fix: Commit the printf harness (a `main` beside bitlen.go, or a `harness/go/bitlen_rows.sh`) and name it in the docstring, so the 'mechanically rewritten' step is repeatable.

## softfloat (2)

- **HIGH** [docdrift] `harness/softfloat/probe_es_unblock.lean:18` — The transcription labelled 'as landed' is the pre-unblock body (`let t := n.toInt64`). The landed `numberToString` is at `LeanModels/Es/Convert.lean:224-238` and reads `let t := n.toModel.toInt64 … Float.ofModel (Float.Model.ofInt64 t)` — i.e. the routed version this probe presents as the alternative. Line 219 is prose, not code. `harness/softfloat/probe_es_unblock_axioms.lean:20` carries the identical stale docstring.
  - fix: Re-point the docstring at `Convert.lean:224-238`, rename the transcribed def to `numberStringPreUnblock`, and retitle §40 'The pre-unblock version' so the landed half is the routed one.
- **MEDIUM** [docdrift] `harness/softfloat/probe_es_unblock.lean:62` — `Convert.lean:303` is `| .str _, _ | _, .str _ =>`, the string-concat arm of `+`; the `%` arm is at :315 and it is WITHDRAWN — :324 reads `SemM.refuseConstruct "`%` needs a non-clamping truncation (SoftFloat's toInt_eq_truncate); refusing rather than clamping"`. The same stale citation appears at `harness/softfloat/probe_es_unblock_axioms.lean:59`.
  - fix: Re-cite `Convert.lean:315-324`, and either retitle the row as 'the shape the WITHDRAWN `%` arm would need' or move it beside `toInt_eq_truncate` in `LeanModels/SoftFloat/Theorems.lean`.

## refuted (8)

- LeanModels/Python/Monadic/Substrate.lean:34 — REFUTED. Quote verified verbatim at LeanModels/Python/Monadic/Substrate.lean:34 ("Zero `sorry`. Zero `native_decide`. `#
- harness/refusal_census.py:959 — REFUTED. Quote is exact at harness/refusal_census.py:958-959 and envelope_for does return None loudly (tools/leanpy:236,
- harness/import_closure.py:114 — REFUTED. The quote is verbatim and the local mechanism is real, but the claimed consequence is gated at the only consume
- harness/c_suite_census.py:177 — REFUTED. The quote is exact and two code observations are true, but the finding's load-bearing evidence is fabricated an
- tools/laws.sh:168 — REFUTED as filed. The quote at tools/laws.sh:168 exists verbatim and the mechanism (per-token sum, no dedup) is real, bu
- LeanModels/Es/Value.lean:12 — REFUTED. Quote exists verbatim (LeanModels/Es/Value.lean:11-13: "and §1.4 measured why the triple / rather than the id a
- harness/diff_test.py:67 — REFUTED. The quote exists verbatim at /Users/ahle/repos/lean-coord/harness/diff_test.py:66-68, but the claimed defect is
- docs/completeness.md:98 — REFUTED — the finding counts in the wrong unit, and the head table it attacks is the explicitly-labelled AS-TAKEN table 